---
phase: 05-market-intel-game-tracker
plan: 08
type: execute
wave: 4
depends_on:
  - 05-03
  - 05-04
  - 05-05
  - 05-06
  - 05-07
files_modified:
  - src/screens/preordain.js
  - src/screens/vandalblast.js
  - src/components/sidebar.js
autonomous: false
requirements:
  - MRKT-01
  - MRKT-02
  - GAME-09
  - GAME-10
  - GAME-13
  - PERF-03

must_haves:
  truths:
    - "Preordain and Vandalblast screens are fully wired and accessible from sidebar"
    - "Sidebar nav items for Preordain and Vandalblast are unlocked (not greyed out)"
    - "Full game lifecycle works: setup -> play -> end -> save -> history"
    - "Spoiler browser, watchlist, and movers tabs all function"
    - "Mobile layout works at 375px, 768px, 1024px breakpoints"
  artifacts:
    - path: "src/screens/preordain.js"
      provides: "Complete Preordain screen with all tabs wired"
    - path: "src/screens/vandalblast.js"
      provides: "Complete Vandalblast screen with all views wired"
  key_links:
    - from: "src/screens/preordain.js"
      to: "src/components/watchlist-panel.js"
      via: "import renderWatchlistPanel"
      pattern: "renderWatchlistPanel"
    - from: "src/screens/vandalblast.js"
      to: "src/components/floating-toolbar.js"
      via: "import renderFloatingToolbar"
      pattern: "renderFloatingToolbar"
---

<objective>
Integration wiring, sidebar unlock, and visual/functional verification checkpoint.

Purpose: Wire all Phase 5 components into their screens, unlock sidebar navigation for Preordain and Vandalblast, and verify the complete end-to-end experience.

Output: Fully integrated Preordain and Vandalblast screens with visual checkpoint.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-market-intel-game-tracker/05-CONTEXT.md
@.planning/phases/05-market-intel-game-tracker/05-UI-SPEC.md
@.planning/phases/05-market-intel-game-tracker/05-03-SUMMARY.md
@.planning/phases/05-market-intel-game-tracker/05-04-SUMMARY.md
@.planning/phases/05-market-intel-game-tracker/05-05-SUMMARY.md
@.planning/phases/05-market-intel-game-tracker/05-06-SUMMARY.md
@.planning/phases/05-market-intel-game-tracker/05-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire remaining components into screens and unlock sidebar</name>
  <files>src/screens/preordain.js, src/screens/vandalblast.js, src/components/sidebar.js</files>
  <read_first>src/screens/preordain.js, src/screens/vandalblast.js, src/components/sidebar.js, src/components/watchlist-panel.js, src/components/movers-panel.js, src/components/floating-toolbar.js, src/components/post-game-overlay.js, src/components/game-history-view.js</read_first>
  <action>
**1. Preordain screen final wiring** (`src/screens/preordain.js`):
Ensure all tab content areas render the correct component:
- Spoilers tab: `renderSpoilerGallery()` (should already be wired from Plan 03)
- Watchlist tab: import and render `renderWatchlistPanel()` from `../components/watchlist-panel.js`
- Movers tab: import and render `renderMoversPanel()` from `../components/movers-panel.js`

Verify the tab switching with `x-show` or `x-if` based on `$store.market.activeTab`:
```html
<div x-show="$store.market.activeTab === 'spoilers'">...spoiler gallery...</div>
<div x-show="$store.market.activeTab === 'watchlist'">...watchlist panel...</div>
<div x-show="$store.market.activeTab === 'movers'">...movers panel...</div>
```

Ensure market store init is called on mount and cleanup is registered.

**2. Vandalblast screen final wiring** (`src/screens/vandalblast.js`):
Ensure all views and sub-views are wired:
- Active Game view:
  - Setup: `renderGameSetup()` (should be wired from Plan 05)
  - Active: `renderPlayerGrid()` from player-card.js + `renderFloatingToolbar()` from floating-toolbar.js
  - Summary: `renderPostGameOverlay()` from post-game-overlay.js
- History view: `renderGameHistoryView()` from game-history-view.js

Wire the floating toolbar: import `renderFloatingToolbar` and include it in the screen when `$store.game.view === 'active'`. The toolbar should be appended to the DOM (not inside the scrollable content area) since it's position: fixed.

Wire the post-game overlay: import `renderPostGameOverlay` and include it. It manages its own visibility via `$store.game.view === 'summary'`.

Ensure game store init is called on mount and cleanup is registered (including timer interval cleanup).

**3. Sidebar unlock** (`src/components/sidebar.js`):
Read the sidebar component. Find where Preordain and Vandalblast nav items are rendered (they may have a "locked" or "greyed out" state). Remove any disabled/locked styling or conditions. Ensure both items navigate properly:
- Preordain: navigates to `#/preordain`
- Vandalblast: navigates to `#/vandalblast`
- Both should have active state highlighting when on their route

Add the alert badge dot for Preordain (if not already done in Plan 04 -- verify and ensure it's present): show `.badge-alert` when `$store.market.alertBadgeCount > 0`.
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - src/screens/preordain.js imports and renders `renderWatchlistPanel` and `renderMoversPanel`
    - src/screens/preordain.js contains all three tab show conditions: `activeTab === 'spoilers'`, `activeTab === 'watchlist'`, `activeTab === 'movers'`
    - src/screens/vandalblast.js imports `renderFloatingToolbar`, `renderPostGameOverlay`, `renderGameHistoryView`
    - src/screens/vandalblast.js contains view conditionals for 'setup', 'active', 'summary'
    - src/components/sidebar.js does NOT contain any disabled/locked state for Preordain or Vandalblast
  </acceptance_criteria>
  <done>All Phase 5 components wired into screens, sidebar unlocked for both new screens</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual and functional verification of complete Phase 5</name>
  <files>src/screens/preordain.js, src/screens/vandalblast.js</files>
  <action>
Human verifies the complete Preordain (Market Intel) and Vandalblast (Game Tracker) screens:
- Preordain: Release calendar, spoiler browser with filters + NEW badge, price watchlist with sparklines, market movers, price alerts with badge
- Vandalblast: Game setup, player card grid with life/poison/commander-damage/tax, long-press acceleration, floating toolbar with dice/coin/timer/counters, post-game summary with life chart, game history with stats
- Both screens accessible from sidebar
- Vandalblast responsive at mobile breakpoints

Steps:
1. Start dev server: `npm run dev`
2. Preordain screen: Click Preordain in sidebar. Verify release calendar, tabs, spoiler browser, watchlist empty state, movers empty state.
3. Vandalblast screen: Click Vandalblast in sidebar. Create game, test life adjustments, long-press, commander damage, dice/coin/timer, end game, save, check history.
4. Mobile test: Resize to 375px width on Vandalblast. Verify single-column layout, floating toolbar, touch targets.
5. Sidebar: Verify both icons active (not greyed).
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - User confirms Preordain screen renders with release calendar, 3 working tabs
    - User confirms Vandalblast game lifecycle works end to end
    - User confirms mobile layout at 375px is usable
    - User confirms sidebar navigation unlocked for both screens
  </acceptance_criteria>
  <done>User has approved the visual and functional state of both Phase 5 screens</done>
</task>

</tasks>

<verification>
- All tests pass: `npx vitest run`
- Both screens fully functional via sidebar navigation
- Mobile layout verified at 375px, 768px, 1024px
- Full game lifecycle: setup -> play -> end -> save -> history review
</verification>

<success_criteria>
- Preordain screen: calendar + 3 tabs fully functional
- Vandalblast screen: complete game lifecycle working
- Sidebar navigation unlocked for both screens
- Mobile responsive layout for Vandalblast
- All existing tests still pass (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/05-market-intel-game-tracker/05-08-SUMMARY.md`
</output>
