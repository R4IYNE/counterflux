---
phase: 1
slug: foundation-data-layer
status: draft
shadcn_initialized: false
preset: none
created: 2026-04-03
revised: 2026-04-03
---

# Phase 1 — UI Design Contract

> Visual and interaction contract for the Foundation + Data Layer phase. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (vanilla JS + Alpine.js, no component library) |
| Preset | not applicable |
| Component library | none (custom components, Organic Brutalism system) |
| Icon library | Material Symbols Outlined (font, `FILL` 0, `wght` 400, `opsz` 24) |
| Specialty icon fonts | mana-font (mana symbols), keyrune (set symbols) |
| Display font | Syne (self-hosted .woff2, 400/700) |
| Body font | Space Grotesk (self-hosted .woff2, 400/700) |
| Mono font | JetBrains Mono (self-hosted .woff2, 400/700) |
| Serif accent | Crimson Pro (self-hosted .woff2, 400/700) — reserved for future phases |
| CSS framework | Tailwind CSS v4 (CSS-first config via `@theme`, no `tailwind.config.js`) |
| Border radius | 0px everywhere (Organic Brutalism, no exceptions) |

**Font weight rationale:** All font families ship exactly 2 weights — 400 (regular) and 700 (bold). The original Stitch mockup used 800 for Syne display text; this is consolidated to 700 for weight budget compliance. Visual impact is maintained through 48px size and -0.02em letter-spacing. The original 13px body-sm at JetBrains Mono 700 is absorbed into the label tier at 11px/700, and the original 10px label-sm is absorbed into label at 11px/700. The original 9px caption tier is absorbed into label at 11px/400.

**Source:** CONTEXT.md D-13, D-16, D-17; RESEARCH.md Standard Stack; DESIGN.md sections 1, 3, 5

---

## Spacing Scale

Declared values (multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline element padding, indicator dots |
| sm | 8px | Compact element spacing, sidebar nav item vertical padding, stack monitor item gaps |
| md | 16px | Default element spacing, card content separation |
| lg | 24px | Section padding, card-to-card vertical spacing |
| xl | 32px | Layout column gaps, main content padding |
| 2xl | 48px | Major section breaks, sidebar top padding |
| 3xl | 64px | Page-level vertical spacing |

Exceptions: none. Sidebar nav items use 8px vertical padding (py-2), replacing the original Stitch 12px (py-3) which fell outside the standard scale. Touch target minimum not applicable (desktop-first).

**Source:** DESIGN.md sections 4, 5; Stitch code.html spacing patterns (12px adjusted to 8px for scale compliance)

---

## Typography

### Font Size Scale

| Token | Size | Font | Weight | Line Height | Letter Spacing | Transform | Usage |
|-------|------|------|--------|-------------|----------------|-----------|-------|
| display | 48px | Syne | 700 | 1.1 | -0.02em | none | Portfolio totals, hero numbers |
| heading | 20px | Syne | 700 | 1.2 | 0.01em | none | Section titles, panel headers, app bar title |
| body | 14px | Space Grotesk | 400 | 1.5 | normal | none | Oracle text, descriptions, Mila messages |
| label | 11px | JetBrains Mono | 400 or 700 | 1.3 | 0.15em | uppercase | Sidebar nav items, search input, overline labels, section labels, button text, timestamps, version strings, stat labels, metadata, card names in lists |

### Label Weight Guidance

The label tier uses both declared weights contextually:

| Context | Weight | Example |
|---------|--------|---------|
| Data values, nav items, timestamps, metadata | 400 | Sidebar labels, activity log body, search input text |
| Overline labels, button text, section headers, card names | 700 | "PORTFOLIO STATUS // AETHERFLOW", "RETRY DOWNLOAD", deck grid titles |

### Rules

- JetBrains Mono is ALWAYS uppercase when used for labels, metadata, and navigation (D-18)
- Space Grotesk is the only font used in sentence case (body text, Mila dialogue)
- Syne is used sparingly — display numbers and section headings only
- Crimson Pro reserved for future serif accent (flavour text quotes in later phases)
- No font size smaller than 11px
- Maximum 2 weights across the entire scale: 400 and 700

**Source:** CONTEXT.md D-16, D-18; DESIGN.md section 3; Stitch code.html extracted sizes (consolidated from 8 tiers to 4)

---

## Color

### Palette

| Token | Value | CSS Custom Property |
|-------|-------|-------------------|
| primary | #0D52BD | `--color-primary` |
| secondary | #E23838 | `--color-secondary` |
| background | #0B0C10 | `--color-background` |
| surface | #14161C | `--color-surface` |
| surface-hover | #1C1F28 | `--color-surface-hover` |
| border-ghost | #2A2D3A | `--color-border-ghost` |
| text-primary | #EAECEE | `--color-text-primary` |
| text-muted | #7A8498 | `--color-text-muted` |
| text-dim | #4A5064 | `--color-text-dim` |
| success | #2ECC71 | `--color-success` |
| warning | #F39C12 | `--color-warning` |
| glow-blue | rgba(13, 82, 189, 0.3) | `--color-glow-blue` |
| glow-red | rgba(226, 56, 56, 0.25) | `--color-glow-red` |

### 60/30/10 Distribution

| Role | Tokens | Usage |
|------|--------|-------|
| Dominant (60%) | background (#0B0C10), surface (#14161C) | Page background, main content area, sidebar background |
| Secondary (30%) | surface-hover (#1C1F28), border-ghost (#2A2D3A) | Card/panel backgrounds, data grids, ghost borders for containment, topbar glass overlay |
| Accent (10%) | primary (#0D52BD) | See reserved list below |

### Accent (#0D52BD) Reserved For

- Active sidebar nav item (background tint + right border + text colour)
- Section overline labels (text colour)
- Search input focus ring and border
- Aether glow box-shadow on panels
- "Initialize Ritual" button background (Phase 3 preview)
- Scanline animation gradient
- Cyclone logo icon tint

### Secondary Accent (#E23838) Reserved For

- "Cast Spell" / destructive action button background
- Terminal Quickcast panel overline
- Error toast left border

### Semantic Colours

| State | Colour | Usage |
|-------|--------|-------|
| Success | #2ECC71 | Success toast, positive price change badge, data sync confirmation |
| Warning | #F39C12 | Warning toast, stale data indicator |
| Error/Destructive | #E23838 | Error toast, failed download, destructive confirmation |

### Surface Hierarchy (No-Line Rule)

Boundaries are defined by tonal shifting, NOT borders:

| Layer | Token | When to Use |
|-------|-------|-------------|
| Base | background (#0B0C10) | Page background, sidebar background |
| Content | surface (#14161C) | Panels, cards, content containers |
| Raised | surface-hover (#1C1F28) | Hover states, elevated cards, nested containers |
| Ghost border | border-ghost (#2A2D3A) | Data grids, explicit containment (1px, NOT for layout) |

**Source:** CONTEXT.md D-22, D-12, D-13, D-14; DESIGN.md sections 2, 4; Stitch code.html Tailwind config

---

## Copywriting Contract

### Phase 1 Screens

| Element | Copy |
|---------|------|
| App title (topbar) | `AETHERIC TERMINAL V3` (Syne, uppercase) |
| Sidebar brand | `ARCHIVE` (mono, tracking-[0.3em]) |
| Version string | `V.03.0-AETHERIC` (label size, text-dim) |

### Splash Screen (First Load)

| Element | Copy |
|---------|------|
| Heading | `Initializing Aetheric Archive...` |
| Progress label | `DOWNLOADING BULK DATA: {downloaded}MB / {total}MB` |
| Parsing label | `PARSING CARD DATABASE: {parsed} / {total} CARDS` |
| Flavour text (rotating) | Rotate 3-5 MTG flavour text quotes, e.g. `"The Izzet are creative geniuses. Disregard the number of property-loss inquiries." -- Razia, Boros Archangel` |
| Completion | `ARCHIVE READY. {total} CARDS INDEXED.` |

### Welcome Landing (Main Content)

| Element | Copy |
|---------|------|
| Primary CTA | Search bar with placeholder: `QUERY DATABASE...` |
| Welcome heading | `Welcome to the Aetheric Archive` |
| Welcome body (Mila) | `Mila here! Your card database is ready. Start typing a card name above to search {total} cards from {set_count} sets.` |

### Search Autocomplete

| Element | Copy |
|---------|------|
| No results | `No cards match your query.` |
| Loading | `Searching archive...` |
| Result count | `{count} results` (label size, text-dim) |

### Empty States (Placeholder Screens)

| Screen | Heading | Body |
|--------|---------|------|
| Epic Experiment | `Dashboard Coming Soon` | `Mila is still calibrating the experiment. This screen will show your portfolio summary, activity feed, and deck quick-launch when Phase 6 is complete.` |
| Thousand-Year Storm | `Deck Builder Coming Soon` | `The storm hasn't gathered yet. This screen will let you build Commander decks with live analytics when Phase 3 is complete.` |
| Treasure Cruise | `Collection Manager Coming Soon` | `No treasures catalogued yet. This screen will track your entire MTG collection when Phase 2 is complete.` |
| Preordain | `Market Intel Coming Soon` | `The future is still hazy. This screen will show spoilers, price watchlists, and market trends when Phase 5 is complete.` |
| Vandalblast | `Game Tracker Coming Soon` | `No games in the archive. This screen will track life totals, commander damage, and game history when Phase 5 is complete.` |

### Card Detail Flyout

| Element | Copy |
|---------|------|
| Panel title | `CARD DETAIL` (label, weight 700, overline) |
| Action button 1 | `Add to Collection` (disabled in Phase 1, tooltip: "Available in Phase 2") |
| Action button 2 | `View on Scryfall` (opens external link) |
| Price label | `MARKET PRICE` (label) |
| Legalities header | `FORMAT LEGALITIES` (label, weight 700) |

### Toast Notifications

| Type | Icon | Example Copy |
|------|------|-------------|
| Info | `info` | `Background data refresh started.` |
| Success | `check_circle` | `Card database updated. {count} cards indexed.` |
| Warning | `warning` | `Card database is {days} days old. Refresh recommended.` |
| Error | `error` | `Bulk data download failed. Check your connection and try again.` |

### Error States

| Scenario | Copy |
|----------|------|
| Bulk download network failure | `Connection lost during download. {downloaded}MB of {total}MB received. Retrying in {seconds}s...` |
| IndexedDB quota exceeded | `Storage limit reached. Your browser may be low on disk space. Try clearing other site data.` |
| Persistent storage denied | `Storage permission denied. Your data may be cleared by the browser after inactivity. This won't affect your session.` |
| Search index empty | `Card database not loaded. Please wait for the initial download to complete.` |

### Destructive Actions

No destructive actions exist in Phase 1 (no user data to delete). Destructive confirmation patterns will be defined in Phase 2 (collection deletion) and Phase 3 (deck deletion).

**Source:** CONTEXT.md D-02, D-05, D-06, D-09, D-10; REQUIREMENTS.md SHELL-07, MILA-02; Stitch code.html copy patterns

---

## Component Inventory (Phase 1)

Components the executor must build for this phase:

| Component | Alpine Store | Key Interactions |
|-----------|-------------|-----------------|
| `splash-screen` | `bulkdata` | Progress bar, download stats, rotating flavour text. Blocks all interaction until complete. Fades out on completion. |
| `sidebar` | `app` | 5 nav items (1 active, 4 locked/greyed). Cyclone icon + "ARCHIVE" brand. Version string. Mila avatar at bottom. Collapse to 64px icon rail below 1024px viewport. Active item: `bg-primary/10 text-primary border-r-4 border-primary`. Locked items: `text-text-dim cursor-not-allowed opacity-50`. **Collapsed state accessibility:** Each nav icon has an `aria-label` attribute matching its full label text (e.g. `aria-label="Epic Experiment"`). A CSS tooltip appears on hover showing the label text — implemented via `::after` pseudo-element with `content: attr(aria-label)`, positioned right of the icon, styled with `surface-hover` background and `text-primary` colour. Tooltip uses `label` typography (11px JetBrains Mono 400 uppercase). |
| `topbar` | `app`, `search` | Glass overlay (`backdrop-filter: blur(12px)`, `bg-background/85`). Syne title left. Search input centre. Notification bell + Mila avatar right. Fixed position, `h-16`, `z-40`. |
| `autocomplete` | `search` | Debounced input (150ms). Dropdown below search bar. Each result: card name + thumbnail (32px) + set icon (keyrune) + mana cost (mana-font). Max 8 results visible. Keyboard navigation (arrow keys + Enter). `Escape` closes. |
| `card-flyout` | `search` | Slide-in from right, 400px wide. Full card image (Scryfall `normal` size, 488x680). Oracle text, type line, mana cost, price, legalities grid. "View on Scryfall" button. Close via X button or `Escape`. Glass overlay backdrop. |
| `toast` | `app` | Bottom-right stack. Max 3 visible. Auto-dismiss 5s. Left colour border (primary=info, success=success, warning=warning, secondary=error). Material Symbol icon. Close X button. Slide-in from right animation. |
| `empty-state` | none | Centred in content area. Mila illustration (grayscale). Heading + body text. No action button (screens are locked). |
| `mila` | none | Sidebar: 40px avatar, grayscale by default, colour on hover. Loading: CSS pulse animation fallback (sprite sheet TBD from user). |
| `welcome` | `search`, `bulkdata` | Hero layout: Counterflux wordmark, Mila greeting, prominent search bar. Card count stat. Centred in main content area. |

---

## Interaction Patterns (Phase 1 Scope)

### Navigation

| Interaction | Behaviour |
|-------------|-----------|
| Click active nav item | No-op (already on this screen) |
| Click locked nav item | No-op, cursor shows `not-allowed` |
| Hover unlocked nav item | Background shifts to `primary/5`, text shifts to primary |
| Route change | Instant content swap, no transition animation (D-11). Hash URL updates. Scroll resets to top. |
| Sidebar collapse | Below 1024px viewport width, sidebar collapses to 64px. Labels hidden, icons only. Mila avatar shrinks to 32px. Each icon displays `aria-label` and CSS tooltip on hover (see Component Inventory). |

### Search

| Interaction | Behaviour |
|-------------|-----------|
| Type in search | Debounce 150ms, then query Dexie.js. Show autocomplete dropdown. |
| Arrow keys in dropdown | Navigate suggestions. Active suggestion highlighted with `surface-hover` background. |
| Enter on suggestion | Open card detail flyout for selected card. |
| Click suggestion | Open card detail flyout for clicked card. |
| Escape | Close autocomplete dropdown. If flyout open, close flyout. |
| `/` key (global) | Focus search input (Phase 6 requirement UX-01, but implement early as it's trivial). |

### Flyout

| Interaction | Behaviour |
|-------------|-----------|
| Open | Slide in from right edge, 300ms ease-out. Glass overlay backdrop covers main content. |
| Close | Click X, press Escape, or click backdrop. Slide out right, 200ms ease-in. |
| "View on Scryfall" | Opens `scryfall.com/card/{set}/{collector_number}` in new tab. |
| "Add to Collection" | Disabled button with tooltip "Available in Phase 2". |

### Splash Screen

| Interaction | Behaviour |
|-------------|-----------|
| During download | Full-screen overlay. Progress bar fills. MB counter updates. Flavour text rotates every 8s. No user interaction possible. |
| On completion | 1s delay showing "ARCHIVE READY" message, then splash fades out (300ms opacity transition). |
| On error | Error message with retry button. "RETRY DOWNLOAD" button resets and restarts the pipeline. |

### Toast

| Interaction | Behaviour |
|-------------|-----------|
| Appear | Slide in from right, 200ms. Stack from bottom-right. |
| Auto-dismiss | Fade out after 5s. Pause timer on hover. |
| Close | Click X button. Slide out right, 150ms. |
| Stack overflow | Max 3 visible. Oldest dismissed when 4th arrives. |

---

## CSS Utilities Contract

Custom utility classes to implement (from Stitch code.html):

| Class | CSS |
|-------|-----|
| `.ghost-border` | `border: 1px solid #2A2D3A;` |
| `.mono-data` | `font-family: 'JetBrains Mono', monospace;` |
| `.syne-header` | `font-family: 'Syne', sans-serif;` |
| `.glass-overlay` | `backdrop-filter: blur(12px); background-color: rgba(11, 12, 16, 0.85);` |
| `.aether-glow` | `box-shadow: 0 0 15px rgba(13, 82, 189, 0.15);` |
| `.scanline` | Absolute positioned 100px tall gradient strip, animates top-to-bottom over 8s, `opacity: 0.1`. Optional decorative effect (Claude's discretion). |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| Not applicable | N/A | No component registry (vanilla JS project) |

NPM packages are vetted in RESEARCH.md. All are established open-source libraries with 1000+ GitHub stars.

---

## Responsive Breakpoints

| Breakpoint | Sidebar | Topbar | Content |
|------------|---------|--------|---------|
| >= 1024px | 240px expanded, labels visible | Left offset 240px | `ml-64` |
| < 1024px | 64px collapsed, icons only with `aria-label` + CSS tooltip on hover | Left offset 64px | `ml-16` |

Desktop-first. No mobile layout required for Phase 1 (GAME-13 mobile layout is Phase 5 only).

---

## Asset Dependencies

| Asset | Status | Fallback |
|-------|--------|----------|
| Mila Izzet engineer (`assets/assetsmila-izzet.png`) | Available | N/A |
| Mila sprite sheet (loading animation) | NOT YET PROVIDED by user | CSS pulse animation on static Mila image |
| Mila transparent background version | NOT YET PROVIDED | Use original on dark background (black bg blends with app background #0B0C10) |
| Self-hosted .woff2 fonts (Syne, Space Grotesk, JetBrains Mono, Crimson Pro) | Must be sourced from Google Fonts repo | System font fallback stack |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
