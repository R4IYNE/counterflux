---
phase: 08.1-treasure-cruise-polish-precon-coverage
plan: 02
subsystem: collection
tags: [precons, scryfall, alpine, dexie, vitest, tdd]

# Dependency graph
requires:
  - phase: 08-treasure-cruise-rapid-entry
    provides: Phase 8 Plan 3 baseline — fetchPrecons + fetchPreconDecklist
              + isCommander heuristic + addAllFromPrecon transaction +
              precon-browser VIEW A/B render
  - phase: 08.1-treasure-cruise-polish-precon-coverage
    provides: Plan 1 (re-open affordance) and Plan 3 (hover-checkbox)
              committed and stable in master prior to this plan landing
provides:
  - PRECON_EXTRA_CODES — code-level allowlist of 18 confirmed Commander/
    multiplayer precon products living under non-commander set_types
  - isMultiDeckBundle(precon) — defensive helper detecting > 200-card
    decklists (multi-deck Scryfall sets)
  - addAllFromPrecon early-return guard — silent no-op on bundled
    products (zero writes, zero side effects)
  - precon-browser VIEW B multi-deck warning state — replaces 1000-card
    list with MULTI-DECK PRODUCT heading + body copy + OPEN IN SCRYFALL
    link; ADD ALL button greyed and labelled MULTI-DECK PRODUCT
  - mockBundlePages(code) shared fixture — single source of truth for
    > 200-card decklist seeds in Vitest
affects: [phase-08.2-precon-deck-curation, phase-09-deck-builder]

# Tech tracking
tech-stack:
  added: []  # purely additive feature work, no new dependencies
  patterns:
    - "Allowlist sidesteps over-broad set_type widening — surgical
       inclusion of confirmed precon codes without triggering 60+
       false-positive draft/reprint sets"
    - "Helper-on-window bridge for Alpine x-data scope — Alpine x-data
       string templates can't import ES modules; assigning the helper
       to window once at render time mirrors Phase 8's existing
       __cf_getPreconCardName / __cf_hydratePreconNames pattern"
    - "Early-return guard before transaction — bundle check sits between
       the no-decklist throw and the Dexie transaction so zero side
       effects occur when bundled (no writes, no reload, no toast,
       no undo entry, browser stays open)"

key-files:
  created: []
  modified:
    - src/services/precons.js
    - src/components/precon-browser.js
    - src/stores/collection.js
    - tests/precons.test.js
    - tests/collection.precon.test.js
    - tests/fixtures/scryfall-precons.js

key-decisions:
  - "Allowlist is code-level (PRECON_EXTRA_CODES) not set_type widening —
     prevents 60+ unwanted draft/reprint sets like Mystery Booster 2 from
     leaking into the precon browser; mb2 negative-control fixture proves
     the surgical filter via Test A3"
  - "Bundle threshold = 200 cards exactly — catches all 4-deck modern
     bundles (Doctor Who 1178, Fallout 1068, Warhammer 40K 617, ToME
     591, Final Fantasy 486, Commander Masters 1067) and older 5-deck
     Commander 2013 (356); only false negative possible if Scryfall
     ever ships a 199-card single deck (none observed)"
  - "Existing cmm fixture row was RECLASSIFIED in place from
     set_type 'commander' to set_type 'masters' rather than appending
     a duplicate — the row was previously mis-typed; Scryfall actually
     classifies Commander Masters as 'masters'. cmm now flows through
     the allowlist path, dogfooding the FOLLOWUP-4A code path"
  - "addAllFromPrecon early-returns silently with NO toast — the
     warning state in precon-browser already communicates the situation;
     firing a toast on top would be redundant noise"
  - "Browser stays OPEN on bundled-precon early-return so the warning
     copy stays visible — user navigates back via the BACK button or
     close X manually (Test C5 locks this behaviour)"

patterns-established:
  - "Pattern: Helper-on-window for Alpine x-data — assign module
     functions to window once at render time so x-data string
     templates can read them via window.__cf_*"
  - "Pattern: Multi-step TDD per task — RED commit (failing tests
     only) followed by GREEN commit (minimal implementation), each
     with --no-verify per executor protocol"
  - "Pattern: Shared fixture as single source of truth — mockBundlePages
     exported from tests/fixtures/scryfall-precons.js so tests
     consuming the same bundle pattern stay aligned (no inline
     for-loops duplicating fixture data)"

requirements-completed:
  - FOLLOWUP-4A
  - FOLLOWUP-4B

# Metrics
duration: 6m 29s
completed: 2026-04-16
---

# Phase 08.1 Plan 02: Precon Allowlist + Multi-Deck Bundle Guard Summary

**18-code PRECON_EXTRA_CODES allowlist surfaces previously-hidden Commander Masters, Commander Legends I+II, Planechase, Archenemy, Premium Deck Series, Commander's Arsenal, Commander Collection, Game Night, and the ToME Deluxe Commander Kit — paired with an isMultiDeckBundle(>200) guard that swaps the 1000-card decklist render for a MULTI-DECK PRODUCT warning and silently no-ops addAllFromPrecon.**

## Performance

- **Duration:** 6m 29s
- **Started:** 2026-04-16T13:34:51Z
- **Completed:** 2026-04-16T13:41:20Z
- **Tasks:** 2 (TDD — 4 commits total: RED + GREEN per task)
- **Files modified:** 6

## Accomplishments

- Tier A (FOLLOWUP-4A): 18-code surgical allowlist landed in
  `src/services/precons.js`. `fetchPrecons` filter now passes any set
  whose `code ∈ PRECON_EXTRA_CODES` OR whose `set_type ∈
  PRECON_SET_TYPES`. Mystery Booster 2 (mb2 — same set_type as cmm but
  not in the allowlist) is correctly excluded by Test A3, proving the
  filter is code-level surgical not set_type widening.
- Tier B (FOLLOWUP-4B): `isMultiDeckBundle(precon)` exported from
  precons.js as a defensive helper (handles missing/null/empty
  decklists). `addAllFromPrecon` early-returns silently when the guard
  trips — zero Dexie writes, zero loadEntries reload, zero toast, zero
  undo entries.
- Multi-deck warning UI rendered into precon-browser VIEW B: yellow
  MULTI-DECK PRODUCT heading + body copy "This product contains
  multiple decks — open in Scryfall to pick a specific deck." + cards-
  count subline + OPEN IN SCRYFALL link to scryfall.com/sets/{code}.
  ADD ALL button label flips to MULTI-DECK PRODUCT when bundled and
  visually disabled (greyed, opacity 0.5, cursor not-allowed, tooltip
  explains the situation).
- Test suite delta: +14 tests (5 FOLLOWUP-4A + 3 isMultiDeckBundle +
  5 bundle-guard C1-C5 + 1 render). +3 existing test count
  assertions updated (Test 1 length 5→9, Test 1 setTypes set extended
  to 7 distinct types, Test 4 length 5→9). dd2-as-oldest assertion
  preserved unchanged (dd2 2008-11-07 is still older than arc
  2010-06-18).

## Task Commits

Each task was committed atomically per TDD protocol (RED → GREEN):

1. **Task 1 RED: failing tests for allowlist + helper** — `009c8f2` (test)
2. **Task 1 GREEN: PRECON_EXTRA_CODES + isMultiDeckBundle** — `8b25795` (feat)
3. **Task 2 RED: failing tests for bundle guard + render** — `6ad6f23` (test)
4. **Task 2 GREEN: bundle-warning + addAllFromPrecon guard** — `b9981b0` (feat)

## Files Created/Modified

- `src/services/precons.js` — PRECON_EXTRA_CODES exported (18 codes); MULTI_DECK_BUNDLE_THRESHOLD (200) constant; isMultiDeckBundle defensive helper exported; fetchPrecons filter line updated to apply both predicates disjunctively
- `src/components/precon-browser.js` — `import { isMultiDeckBundle } from '../services/precons.js'`; window.__cf_isMultiDeckBundle bridge in renderPreconBrowser; isBundle getter in VIEW B x-data; ADD ALL button disabled/relabelled when bundled; multi-deck warning template inserted; decklist-rows guard extended with `&& !isBundle`
- `src/stores/collection.js` — `isMultiDeckBundle` added to the precons.js import; bundle guard early-return inserted in `addAllFromPrecon` between the no-decklist throw and the Dexie transaction (zero side effects on early-return)
- `tests/precons.test.js` — Test 1 length 5→9 + setTypes set extended; Test 4 length 5→9 (×2 assertions); FOLLOWUP-4A describe block (5 tests A1-A5); FOLLOWUP-4B isMultiDeckBundle describe block (3 tests)
- `tests/collection.precon.test.js` — `import { mockBundlePages }`; FOLLOWUP-4B describe block (5 tests C1-C5); render-level describe block (1 test asserting warning copy)
- `tests/fixtures/scryfall-precons.js` — cmm row reclassified from set_type 'commander' to 'masters' (matches Scryfall reality); 4 new allowlist coverage rows (clb, pca, arc, pltc); 1 negative-control row (mb2 — set_type masters but NOT allowlisted); mockBundlePages(code) helper exported (250-card synthetic decklist single source of truth)

## Decisions Made

All key decisions documented in the plan-level `must_haves.truths` block were honoured exactly. Specific points worth reinforcing for STATE.md:

- **Allowlist coverage = exactly 18 codes:** cmm, clb, cmr, pca, pc2, hop, arc, e01, pd2, pd3, h09, cm1, cc1, cc2, gnt, gn2, gn3, pltc. Locked by Test A5 (`expect(PRECON_EXTRA_CODES).toHaveLength(18)`).
- **Bundle threshold = 200 cards (strict greater-than):** Verified by isMultiDeckBundle helper tests — 200 → false, 201 → true. Threshold lives in a private `MULTI_DECK_BUNDLE_THRESHOLD` constant in precons.js (not exported) so callers must use the helper.
- **Reclassified cmm fixture row in place** rather than appending a duplicate — keeps the fixture aligned with Scryfall's actual `set_type: 'masters'` classification and doubles as the FOLLOWUP-4A allowlist proof (Test A1).
- **Bundle copy verbatim:** "This product contains multiple decks — open in Scryfall to pick a specific deck." — body text uses the em-dash variant per the plan `<action>` block.
- **No toast on bundled add-all:** the warning state already communicates the situation in-context; firing a toast on top would be redundant noise. Locked by Test C2.
- **Browser stays OPEN on bundled add-all:** the user keeps seeing the warning copy and navigates back via BACK or close X manually. Locked by Test C5.

## Deviations from Plan

None — plan executed exactly as written. All assertion counts (9 not 5), all 18 allowlist codes, the strict `> 200` threshold, the verbatim warning copy, the dd2-as-oldest preservation, and the cmm reclassification all landed precisely as the plan specified. The retry-context advice to use `--no-verify` was honoured on every commit for consistency with the parallel-execution protocol.

## Issues Encountered

- **One pre-existing test failure** in `tests/router.test.js` (vandalblast screen mount): `TypeError: Cannot read properties of undefined (reading 'data')` at `src/screens/vandalblast.js:17:10`. This failure predates Plan 02 — `vandalblast.js` was last touched in commit `c5defba` from Phase 6, and `deferred-items.md` already lists this as item #1 from the Phase 08.1 Plan 03 execution. Out of scope per the SCOPE BOUNDARY rule (failure is in unrelated code not touched by this plan). No action taken.

## User Setup Required

None — purely additive feature work behind the existing BROWSE PRECONS UI. No new dependencies, no env vars, no service config.

## Manual Dev Walkthrough

Per the plan's `<verification>` block, the following manual steps validate the shipped UX (deferred to user — not run by executor):

1. `npm run dev`, navigate to `/treasure-cruise`, click BROWSE PRECONS (clear localStorage or click REFRESH to bust the precons cache).
2. Confirm new tiles in the grid: Commander Masters (cmm), Commander Legends Battle for Baldur's Gate (clb), Planechase Anthology (pca), Archenemy (arc), Premium Deck Series Slivers (h09), Game Night (gnt), etc.
3. Click any single-deck product (~100 cards) — confirm decklist preview renders normally + ADD ALL active.
4. Click `cmm` (Commander Masters, 1067 cards) — confirm warning state renders, ADD ALL greyed, OPEN IN SCRYFALL link works.
5. Click ADD ALL on a bundled tile — confirm true no-op (collection topbar count unchanged, no toast, no console error).
6. Confirm `mb2` (Mystery Booster 2 — set_type masters, NOT allowlisted) does NOT appear in the grid.

## Self-Check: PASSED

All 6 modified source/test files verified present on disk. All 4 commits
(009c8f2 RED, 8b25795 GREEN, 6ad6f23 RED, b9981b0 GREEN) verified in
`git log --oneline --all`. SUMMARY.md verified present at
`.planning/phases/08.1-treasure-cruise-polish-precon-coverage/08.1-02-SUMMARY.md`.

## Next Phase Readiness

- Phase 08.1 wave 1 work complete: Plan 01 (re-open affordance), Plan 02 (this — allowlist + bundle guard), Plan 03 (hover-checkbox) all shipped.
- Tier C (curated `src/data/precon-decks.js` deck-membership map) deferred to Phase 08.2 per the debug-file recommendation.
- No blockers introduced — Phase 09 (Deck Builder) and Phase 10 (Auth) can proceed against the current baseline.

---
*Phase: 08.1-treasure-cruise-polish-precon-coverage*
*Completed: 2026-04-16*
