---
phase: 05-market-intel-game-tracker
plan: 07
type: execute
wave: 3
depends_on:
  - 05-05
files_modified:
  - src/components/post-game-overlay.js
  - src/components/life-chart.js
  - src/components/game-history-view.js
  - src/components/game-stats-card.js
autonomous: true
requirements:
  - GAME-09
  - GAME-10
  - GAME-11
  - GAME-12
  - PERF-03

must_haves:
  truths:
    - "User sees post-game summary with winner, duration, turns, elimination order"
    - "Life total chart shows one line per player over turns"
    - "Game is saved to IndexedDB linked to deck"
    - "Game history shows aggregate stats and list of past games"
    - "Game Tracker functions fully offline"
  artifacts:
    - path: "src/components/post-game-overlay.js"
      provides: "Full-screen post-game summary"
      exports: ["renderPostGameOverlay"]
    - path: "src/components/life-chart.js"
      provides: "Chart.js line chart for life totals"
      exports: ["renderLifeChart", "destroyLifeChart"]
    - path: "src/components/game-history-view.js"
      provides: "History list with stats and past games"
      exports: ["renderGameHistoryView"]
    - path: "src/components/game-stats-card.js"
      provides: "Small stat display card"
      exports: ["renderGameStatsCard"]
  key_links:
    - from: "src/components/post-game-overlay.js"
      to: "src/stores/game.js"
      via: "$store.game.saveGame"
      pattern: "saveGame"
    - from: "src/components/life-chart.js"
      to: "chart.js"
      via: "LineController registration"
      pattern: "LineController"
    - from: "src/components/game-history-view.js"
      to: "src/stores/game.js"
      via: "$store.game.stats"
      pattern: "\\$store\\.game\\.stats"
---

<objective>
Post-game summary overlay with life chart, game persistence, and game history view with stats.

Purpose: Complete the Game Tracker lifecycle -- post-game summary with winner selection and life total chart, game save to IndexedDB, and history view with aggregate statistics. Also ensures full offline capability for PERF-03.

Output: Post-game overlay, life chart, game history view, stats card components.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-market-intel-game-tracker/05-CONTEXT.md
@.planning/phases/05-market-intel-game-tracker/05-UI-SPEC.md
@.planning/phases/05-market-intel-game-tracker/05-05-SUMMARY.md

<interfaces>
<!-- Game store -->
From src/stores/game.js:
```javascript
Alpine.store('game', {
  view: 'summary',
  players: [{
    name: 'You',
    commander: 'Niv-Mizzet, Parun',
    life_history: [40, 40, 37, 35, ...],
    final_life: 12,
    poison: 0,
    commander_damage: { 1: 5, 2: 0, 3: 21 },
    color_index: 0,
  }],
  currentTurn: 14,
  gameStartedAt: '2026-04-08T19:30:00Z',
  games: [],
  get stats() { return computeGameStats(this.games); },
  async saveGame(winnerIndex, eliminationOrder) { ... },
  async discardGame() { ... },
  async loadHistory() { ... },
  async deleteGame(gameId) { ... },
});
```

<!-- Chart.js pattern -->
From src/components/analytics-panel.js (Chart.js tree-shaken imports):
```javascript
import { Chart, DoughnutController, ArcElement, ... } from 'chart.js';
Chart.register(DoughnutController, ArcElement, ...);
// Instance cleanup: chart.destroy()
```

<!-- Player colours from UI-SPEC -->
Player 1: #0D52BD, Player 2: #E23838, Player 3: #2ECC71,
Player 4: #F39C12, Player 5: #A855F7, Player 6: #22D3EE
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Post-game overlay with winner selection, stats, and life chart</name>
  <files>src/components/post-game-overlay.js, src/components/life-chart.js</files>
  <read_first>src/components/analytics-panel.js, src/components/deck-analytics-panel.js, src/stores/game.js, .planning/phases/05-market-intel-game-tracker/05-UI-SPEC.md</read_first>
  <action>
**1. Life chart** (`src/components/life-chart.js`):
Export `renderLifeChart(canvasId)` and `destroyLifeChart()`.

- Register Chart.js line chart components (tree-shaken, following analytics-panel.js pattern):
```javascript
import { Chart, LineController, LineElement, PointElement, CategoryScale, LinearScale, Tooltip, Legend, Filler } from 'chart.js';
Chart.register(LineController, LineElement, PointElement, CategoryScale, LinearScale, Tooltip, Legend, Filler);
```

- `renderLifeChart(canvasId)`: reads `$store.game.players` data. Creates Chart.js line chart on the canvas element.
  - X-axis: turn numbers (1, 2, 3, ... from life_history length). Category scale. Label tier (11px JetBrains Mono 400). Grid lines: `#2A2D3A` (border-ghost).
  - Y-axis: life total. Linear scale. Label tier. Grid lines: `#2A2D3A`.
  - One dataset per player:
    - Label: player name
    - Data: player's life_history array
    - Border colour: player colour from `PLAYER_COLOURS = ['#0D52BD', '#E23838', '#2ECC71', '#F39C12', '#A855F7', '#22D3EE']` indexed by player's color_index
    - Point radius: 3
    - Border width: 2
    - Fill: false
    - Tension: 0.1 (slight curve)
  - Background: transparent
  - Max height: 400px
  - Tooltip: show all players' life at that turn
  - Legend: show player names with their colours. Label tier font.
- `destroyLifeChart()`: destroy stored chart instance if it exists. MUST be called before creating a new chart or on overlay close.
- Store chart instance in module-level variable for cleanup (same pattern as analytics-panel.js).

**2. Post-game overlay** (`src/components/post-game-overlay.js`):
Export `renderPostGameOverlay()` returning HTML with Alpine bindings.

- Full-screen overlay: `position: fixed; inset: 0; z-index: 50;` Surface background (#14161C). Padding xl (32px). Overflow-y auto.
- Only visible when `$store.game.view === 'summary'`
- Header: `GAME OVER` (display tier, 48px Syne 700, centered)

**Winner selection:**
- Label: `WINNER` (label 700)
- Row of player name buttons. Click to select winner: highlight with primary border. Store selected index in local Alpine data `winnerIndex`.
- Can change selection before saving.

**Game stats:**
- `DURATION` (label 700): compute from gameStartedAt to now (or ended_at). Format as `Xh Ym` or `Xm`.
- `TURNS` (label 700): `$store.game.currentTurn`

**Elimination order:**
- Label: `ELIMINATION ORDER` (label 700)
- Player names listed. Click players in order of elimination (1st eliminated first). Show number badge next to name. Click again to remove from order. Store as array in local Alpine data `eliminationOrder`.

**Life chart:**
- `LIFE TOTAL HISTORY` heading (heading tier)
- `<canvas>` element with id `life-chart-canvas`, max-height 400px
- On overlay show (Alpine `x-init` or `x-effect`): call `renderLifeChart('life-chart-canvas')` after `$nextTick`
- On overlay hide: call `destroyLifeChart()`

**Actions:**
- `Save & Close` button: primary bg, label 700. On click: `$store.game.saveGame(winnerIndex, eliminationOrder)`. Toast: `Game saved to history.`
- `Discard Game` button: secondary bg (#E23838), label 700. On click: show confirmation modal per UI-SPEC copy. On confirm: `$store.game.discardGame()`.

**Read-only mode:** When opened from history (flag like `readOnly`), hide Save/Discard buttons, show only `Close` button.
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - src/components/life-chart.js exports `renderLifeChart` and `destroyLifeChart`
    - src/components/life-chart.js contains `LineController` and `Chart.register`
    - src/components/life-chart.js contains `PLAYER_COLOURS` or player colour hex values `#0D52BD`, `#E23838`, `#2ECC71`
    - src/components/post-game-overlay.js exports `renderPostGameOverlay`
    - src/components/post-game-overlay.js contains `GAME OVER`, `WINNER`, `DURATION`, `TURNS`, `ELIMINATION ORDER`, `LIFE TOTAL HISTORY`
    - src/components/post-game-overlay.js contains `Save & Close` and `Discard Game`
    - src/components/post-game-overlay.js contains `z-index: 50` or `z-50`
    - src/components/post-game-overlay.js contains `destroyLifeChart` call for cleanup
  </acceptance_criteria>
  <done>Post-game overlay with winner/elimination selection, duration/turns stats, life total chart, and Save/Discard actions</done>
</task>

<task type="auto">
  <name>Task 2: Game history view with stats cards and past games list</name>
  <files>src/components/game-history-view.js, src/components/game-stats-card.js</files>
  <read_first>src/stores/game.js, src/utils/game-stats.js, .planning/phases/05-market-intel-game-tracker/05-UI-SPEC.md</read_first>
  <action>
**1. Stats card component** (`src/components/game-stats-card.js`):
Export `renderGameStatsCard(label, valueExpr)` returning HTML string.

- Small surface card (#14161C) with ghost-border, padding sm (8px) horizontal, md (16px) vertical
- Stat label: label 700 uppercase, text-muted, above
- Stat value: heading tier (20px Syne 700), text-primary, below
- valueExpr is an Alpine expression string (e.g., `$store.game.stats.winRate + '%'`)
- Example usage: `renderGameStatsCard('WIN RATE', "$store.game.stats.winRate.toFixed(0) + '%'")`

**2. Game history view** (`src/components/game-history-view.js`):
Export `renderGameHistoryView()` returning HTML with Alpine bindings.

**Stats summary row at top:**
- Flex row (horizontal on >= 768px, wrap on mobile), gap-md
- Six stats cards using renderGameStatsCard:
  1. `WIN RATE`: `$store.game.stats.winRate.toFixed(0) + '%'`
  2. `GAMES PLAYED`: `$store.game.stats.totalGames`
  3. `BEST DECK`: `$store.game.stats.bestDeck?.name || '--'`
  4. `AVG GAME LENGTH`: format `$store.game.stats.avgGameLength` as `Xm` or `Xh Ym`
  5. `MOST PLAYED`: `$store.game.stats.mostPlayed?.commanderName || '--'`
  6. `WIN STREAK`: `$store.game.stats.currentWinStreak`

**Past games list:**
- Header: `PAST GAMES` (heading tier)
- List of game entries from `$store.game.games`, sorted by started_at descending (most recent first)
- Each game row: surface background, ghost-border, padding md, mb-sm. Flex row.
  - Date: format started_at as `DD MMM YYYY` (label 400, text-muted)
  - Your deck/commander: body 400, text-primary, flex-1
  - Opponent count: `vs {N} opponents` (label 400, text-muted)
  - Winner indicator: if winner_index === 0, show `WON` in success colour; else show winner name in text-muted
  - Duration: compute from started_at/ended_at, format as `Xm` (label 400, text-dim)
  - Turn count: `{N} turns` (label 400, text-dim)
- Click row: reopen post-game overlay in read-only mode. Set `$store.game.view = 'summary'` and populate players from the game record. Pass `readOnly: true` flag.
- Delete: X icon on hover (desktop) or visible (mobile). On click: `$store.game.deleteGame(game.id)`. Show undo toast: `Game deleted from history.` with UNDO button (5s timeout).

**Empty state:**
- When games array is empty: Mila empty state with heading `No Games Recorded` and body `Start a game from the Active Game view. Mila will keep track of your victories and defeats.`

**Offline (PERF-03):**
- All game data lives in IndexedDB via Dexie. No API calls during gameplay or history viewing.
- The game store's init() already loads from IndexedDB on mount.
- Note in code comment: `// PERF-03: Game Tracker fully offline — all data from IndexedDB, no network calls`
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - src/components/game-history-view.js exports `renderGameHistoryView`
    - src/components/game-history-view.js contains `PAST GAMES`, `No Games Recorded`, and stats labels (`WIN RATE`, `GAMES PLAYED`, `BEST DECK`)
    - src/components/game-history-view.js contains `deleteGame` for game deletion
    - src/components/game-history-view.js contains `PERF-03` comment
    - src/components/game-stats-card.js exports `renderGameStatsCard`
    - src/components/game-stats-card.js contains label 700 styling pattern (font-mono, 11px, uppercase, letter-spacing)
  </acceptance_criteria>
  <done>Game history with 6 stat cards, past games list with click-to-review, delete with undo, empty state, and offline capability</done>
</task>

</tasks>

<verification>
- `npx vitest run` passes
- Post-game overlay renders with life chart after ending a game
- Game saves to IndexedDB and appears in history
- History stats compute correctly from saved games
- Game Tracker works fully offline (PERF-03)
</verification>

<success_criteria>
- Post-game overlay with winner selection, elimination order, life chart
- Life chart uses Chart.js LineController with player colours
- Save & Close persists game to IndexedDB
- Game history shows 6 stat cards and chronological past games
- Past game click reopens read-only summary
- Delete game with undo toast
- All game functionality offline (no API calls during gameplay)
</success_criteria>

<output>
After completion, create `.planning/phases/05-market-intel-game-tracker/05-07-SUMMARY.md`
</output>
