---
phase: 12-notification-bell-preordain-spoiler-refresh
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/spoiler-set-filter.js
  - tests/spoiler-set-filter.test.js
autonomous: true
requirements:
  - MARKET-01

must_haves:
  truths:
    - "Set filter is a custom Alpine dropdown (not a native <select>), so each option can render a Keyrune icon"
    - "Each option renders `<i class=\"ss ss-{code} ss-fallback\">` alongside the set name and card count"
    - "Clicking an option calls `$store.market.loadSpoilers(code)` and closes the dropdown"
    - "Dropdown dismisses on @click.outside and Escape"
    - "Plan does NOT yet wire the new component into spoiler-gallery.js — Plan 04 owns that swap"
  artifacts:
    - path: "src/components/spoiler-set-filter.js"
      provides: "renderSpoilerSetFilter() export returning x-data HTML for the custom dropdown"
      exports: ["renderSpoilerSetFilter"]
    - path: "tests/spoiler-set-filter.test.js"
      provides: "3+ unit tests covering Keyrune icon render, option selection, name+count display"
      contains: "renderSpoilerSetFilter"
  key_links:
    - from: "src/components/spoiler-set-filter.js"
      to: "$store.market.sets"
      via: "x-for loop over $store.market.sets inside the dropdown list"
      pattern: "\\$store\\.market\\.sets"
    - from: "src/components/spoiler-set-filter.js"
      to: "$store.market.loadSpoilers"
      via: "@click on option button calls loadSpoilers"
      pattern: "\\$store\\.market\\.loadSpoilers"
---

<objective>
Ship `src/components/spoiler-set-filter.js` — a new Alpine-based custom dropdown that replaces the native `<select>` element in `spoiler-gallery.js`. Each option renders a Keyrune set icon (`<i class="ss ss-{code} ss-fallback">`) alongside the set name and card count, fulfilling MARKET-01.

Purpose: Isolate the custom-dropdown precedent into its own module so `spoiler-gallery.js` (Plan 04) can consume it with a single import line. Zero risk of coupling the dropdown's internal state machine to the gallery's render logic.

Output: A standalone `renderSpoilerSetFilter()` export and a dedicated test file. Plan 04 will swap the existing `<select>` for a call to this function.

Scope boundary: This plan creates the component in isolation. The wiring into `src/components/spoiler-gallery.js` happens in Plan 04 alongside the full gallery rewrite — splitting avoids two plans editing the same file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12-notification-bell-preordain-spoiler-refresh/12-CONTEXT.md
@.planning/phases/12-notification-bell-preordain-spoiler-refresh/12-RESEARCH.md
@src/components/precon-browser.js
@src/components/release-calendar.js
@src/components/coin-flipper.js
@src/components/spoiler-gallery.js

<interfaces>
<!-- Existing Keyrune usage — mirror these patterns verbatim -->

<!-- src/components/precon-browser.js:161-163 -->
<i class="ss ss-fallback" :class="'ss-' + precon.code"
   style="position: absolute; top: 50%; left: 50%; ...; font-size: 96px;"></i>

<!-- src/components/release-calendar.js:53,87 -->
<i :class="'ss ss-' + set.code.toLowerCase() + ' ss-2x ss-default'"
   style="color: #EAECEE;"></i>

<!-- Convention (from 12-RESEARCH.md §Pattern 4): always lowercase set.code, always include ss-fallback -->

<!-- Existing @click.outside precedent — src/components/coin-flipper.js:37 -->
<div x-data="{ open: false }" @click.outside="open = false">
  <button @click="open = !open">...</button>
  <div x-show="open">...</div>
</div>

<!-- market.sets shape (from src/services/sets.js via fetchSets()) -->
{ code: string, name: string, card_count: number, released_at: string, ... }

<!-- market.activeSet: string | null — current selection; the filter button shows this set's name -->

<!-- Component contract this plan produces -->
export function renderSpoilerSetFilter(): string
// Returns HTML string with Alpine bindings. Consumed by Plan 04 inside spoiler-gallery.js.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — failing tests for spoiler-set-filter custom dropdown</name>
  <files>tests/spoiler-set-filter.test.js</files>
  <read_first>
    - tests/setup.js (global Alpine stubs)
    - tests/spoiler-filter.test.js (existing spoiler filter test pattern — reuse import/mock style)
    - .planning/phases/12-notification-bell-preordain-spoiler-refresh/12-RESEARCH.md §Pattern 4, §Keyrune Conventions, §Pitfall 2
    - src/components/precon-browser.js (reference for ss-fallback keyrune rendering)
  </read_first>
  <behavior>
    Create `tests/spoiler-set-filter.test.js` with these exact tests:

    1. Test: `renderSpoilerSetFilter returns a string containing x-data with open flag`
       - Assert: return value is a string; contains `x-data="{ open: false`

    2. Test: `renderSpoilerSetFilter includes @click.outside and @keydown.escape.window handlers`
       - Assert: HTML contains literally `@click.outside="open = false"` and `@keydown.escape.window="open = false"`

    3. Test: `renderSpoilerSetFilter renders a keyrune icon element inside the option template`
       - Assert: HTML contains literally `class="ss ss-fallback"` (class attribute for fallback)
       - Assert: HTML contains the pattern `:class="'ss-' + set.code.toLowerCase()"` (dynamic set-code class)

    4. Test: `renderSpoilerSetFilter iterates $store.market.sets`
       - Assert: HTML contains literally `x-for="set in $store.market.sets"`
       - Assert: HTML contains literally `:key="set.code"`

    5. Test: `renderSpoilerSetFilter renders set.name and set.card_count in each option`
       - Assert: HTML contains `x-text="set.name"` (or equivalent binding targeting set.name)
       - Assert: HTML contains `set.card_count` somewhere in a binding expression (wrap format: "(N)" per D-11)

    6. Test: `renderSpoilerSetFilter option click calls $store.market.loadSpoilers(set.code) and closes the dropdown`
       - Assert: HTML contains literally `$store.market.loadSpoilers(set.code)` and (in the same handler) `open = false`

    7. Test: `renderSpoilerSetFilter uses the Neo-Occult Terminal tokens`
       - Assert: HTML contains `#14161C` or `var(--color-surface)` (surface background)
       - Assert: HTML contains `#2A2D3A` or `var(--color-border-ghost)` (ghost border)
       - Assert: HTML contains `#7A8498` or `var(--color-text-muted)` (card count muted colour)

    8. Test: `renderSpoilerSetFilter uses x-cloak to prevent transition flash`
       - Assert: HTML contains `x-cloak` (Pitfall 6 defence from 12-RESEARCH.md)

    Use grep-style regex assertions. No jsdom rendering needed — the component is a string-returning function; static analysis is sufficient.

    Test header:
    ```javascript
    import { describe, it, expect } from 'vitest';
    import { renderSpoilerSetFilter } from '../src/components/spoiler-set-filter.js';
    ```

    All 8 tests MUST fail — the module does not yet exist.
  </behavior>
  <action>
    Create `tests/spoiler-set-filter.test.js` with the 8 tests listed in `<behavior>`. Use a single `describe('renderSpoilerSetFilter', () => { ... })` block.

    For string-content assertions use `expect(html).toContain('literal string')` or `expect(html).toMatch(/regex/)`.

    Run `npm test -- spoiler-set-filter`. Confirm all 8 tests fail with an import error (module not found).

    Commit: `test(12-02): RED — spoiler set filter custom dropdown`
  </action>
  <verify>
    <automated>npm test -- spoiler-set-filter 2>&1 | grep -E "FAIL|failed|Error|Cannot find module" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `tests/spoiler-set-filter.test.js` exists
    - File contains `import { renderSpoilerSetFilter } from '../src/components/spoiler-set-filter.js'`
    - File contains all 8 test identifier strings (`it('...', ...)`)
    - `npm test -- spoiler-set-filter` fails — either "Cannot find module" or "ReferenceError: renderSpoilerSetFilter is not defined"
    - `git log -1 --oneline` shows `test(12-02): RED`
  </acceptance_criteria>
  <done>8 failing tests committed; clear RED state ready for GREEN implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — implement renderSpoilerSetFilter custom Alpine dropdown</name>
  <files>src/components/spoiler-set-filter.js</files>
  <read_first>
    - src/components/spoiler-gallery.js (existing `<select>` at lines 31-41 — new component replaces this markup shape)
    - src/components/precon-browser.js lines 140-180 (ss-fallback + dynamic set-code class reference)
    - src/components/release-calendar.js (keyrune sizing reference)
    - tests/spoiler-set-filter.test.js (verify against)
    - .planning/phases/12-notification-bell-preordain-spoiler-refresh/12-RESEARCH.md §Pattern 4 (complete template sketch)
  </read_first>
  <action>
    Create `src/components/spoiler-set-filter.js` exporting a single function `renderSpoilerSetFilter()` that returns an HTML string. Use the template sketch from `12-RESEARCH.md §Pattern 4` verbatim as a starting point and adapt for the exact test assertions above.

    File header comment:
    ```javascript
    /**
     * Spoiler set-filter custom dropdown — Phase 12 MARKET-01.
     *
     * Replaces the native <select> in spoiler-gallery.js with an Alpine-driven
     * custom dropdown so each option can render a Keyrune set icon (glyphs
     * cannot live inside <option> elements).
     *
     * Consumers: src/components/spoiler-gallery.js (Plan 12-04 wires this in).
     *
     * @returns {string} HTML string with Alpine x-data bindings
     */
    export function renderSpoilerSetFilter() { ... }
    ```

    **Required template contents (every item must be present for tests to pass):**

    1. Root `<div x-data="{ open: false }" class="relative">` — Alpine state at component root.
    2. Trigger `<button>` with:
       - `@click="open = !open"` to toggle
       - `class` including `font-mono text-[11px] uppercase tracking-[0.15em] cursor-pointer px-[8px] py-[4px] outline-none`
       - Inline style: `background: #1C1F28; border: 1px solid #2A2D3A; color: #EAECEE; min-width: 280px;`
       - Display the selected set's name (or fallback `'SELECT SET'`): `x-text="$store.market.activeSet ? ($store.market.sets.find(s => s.code === $store.market.activeSet)?.name || 'SELECT SET') : 'SELECT SET'"`
       - Chevron: `<span class="material-symbols-outlined ml-auto" style="font-size: 16px;">expand_more</span>`
       - `:aria-expanded="open"` for a11y
    3. Dropdown list `<ul x-cloak x-show="open" x-transition.origin.top @click.outside="open = false" @keydown.escape.window="if (open) open = false"` (note the `if (open)` guard from Pitfall 4).
       - Position: `absolute left-0 top-full mt-[4px] w-full max-h-[320px] overflow-y-auto z-30`
       - Style: `background: #1C1F28; border: 1px solid #2A2D3A;`
    4. `<template x-for="set in $store.market.sets" :key="set.code">` iteration.
    5. Each option is `<li><button @click="$store.market.loadSpoilers(set.code); open = false" class="w-full px-[12px] py-[8px] flex items-center gap-[8px] hover:bg-[#2A2D3A] text-left">` with:
       - `<i class="ss ss-fallback" :class="'ss-' + set.code.toLowerCase()" style="font-size: 18px; color: #EAECEE; flex-shrink: 0;"></i>`
       - `<span class="text-[14px] truncate flex-1" style="color: #EAECEE;" x-text="set.name"></span>`
       - `<span class="font-mono text-[11px] tracking-[0.15em]" style="color: #7A8498;" x-text="'(' + set.card_count + ')'"></span>`

    **Edge cases:**
    - The `@click.outside` handler lives on the `<ul>` (dropdown menu), NOT the trigger — per Pitfall 7 from research. This is mandatory.
    - The trigger button shows the active set's Keyrune icon if one is selected: `<template x-if="$store.market.activeSet"><i class="ss ss-fallback" :class="'ss-' + $store.market.activeSet.toLowerCase()" style="font-size: 18px; color: #EAECEE;"></i></template>` — put this before the set-name span in the trigger.

    **Do NOT import anything** — this is a pure template-returning function. Alpine resolves all bindings at render time against `$store.market`.

    Run `npm test -- spoiler-set-filter`. All 8 tests must pass.

    Commit: `feat(12-02): GREEN — spoiler set filter custom keyrune dropdown`
  </action>
  <verify>
    <automated>npm test -- spoiler-set-filter</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/spoiler-set-filter.js` exists
    - File exports `renderSpoilerSetFilter` via `export function renderSpoilerSetFilter`
    - File contains `x-data="{ open: false`
    - File contains `@click.outside="open = false"`
    - File contains `@keydown.escape.window="if (open) open = false"` (exact — Pitfall 4 guard)
    - File contains `class="ss ss-fallback"`
    - File contains `:class="'ss-' + set.code.toLowerCase()"`
    - File contains `x-for="set in $store.market.sets"` and `:key="set.code"`
    - File contains `$store.market.loadSpoilers(set.code); open = false`
    - File contains `x-text="set.name"`
    - File contains `set.card_count`
    - File contains `x-cloak`
    - `npm test -- spoiler-set-filter` exits 0 with 8 passing tests
    - `git log -1 --oneline` shows `feat(12-02): GREEN`
  </acceptance_criteria>
  <done>Custom dropdown component exported and tested. Plan 04 will import it into spoiler-gallery.js.</done>
</task>

</tasks>

<verification>
- `npm test -- spoiler-set-filter` all green
- `npm test` full suite green (no regressions)
- Plan 04 can import `renderSpoilerSetFilter` via `import { renderSpoilerSetFilter } from './spoiler-set-filter.js'`
- Keyrune render pattern matches precon-browser.js and release-calendar.js (ss-fallback always present; lowercase set code)
</verification>

<success_criteria>
- Exported function `renderSpoilerSetFilter()` returns a standalone HTML string
- 8 unit tests enforce the template's structural contract (every Alpine directive, every visible element)
- Component works in isolation — no coupling to spoiler-gallery.js state
- Plan 04 can consume with a single import and one `${renderSpoilerSetFilter()}` template interpolation
</success_criteria>

<output>
After completion, create `.planning/phases/12-notification-bell-preordain-spoiler-refresh/12-02-SUMMARY.md`
</output>
