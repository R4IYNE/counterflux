---
phase: 14-v1-1-audit-gap-closure
plan: 14-04
type: execute
wave: 2
depends_on: [14-01, 14-02, 14-03]
requirements: [SYNC-03, AUTH-06, SCHEMA-01, SCHEMA-02, SCHEMA-03, MARKET-01, PERF-04]
files_modified:
  - .planning/v1.1-MILESTONE-AUDIT.md
  - .planning/STATE.md
autonomous: false
gap_closure: true
must_haves:
  truths:
    - "Re-running /gsd:audit-milestone after 14-01, 14-02, 14-03 land returns status: passed (or at minimum drops Issues A, C, D from the critical/minor lists)"
    - "The original v1.1-MILESTONE-AUDIT.md is preserved (not overwritten) — a close-out addendum is appended documenting Phase 14 resolution"
    - "STATE.md reflects Phase 14 completion + v1.1 milestone ready-to-close"
    - "Issue B (no sign-up UI) is explicitly recorded as deferred to v1.2 with linkage to the deferred-items list"
  artifacts:
    - path: ".planning/v1.1-MILESTONE-AUDIT.md"
      provides: "Original audit PRESERVED + close-out addendum appended with resolution evidence"
      contains: "Phase 14 Close-Out"
    - path: ".planning/STATE.md"
      provides: "Updated milestone progress (Phase 14 complete, v1.1 ready for close)"
      contains: "Phase 14"
  key_links:
    - from: "v1.1-MILESTONE-AUDIT.md close-out section"
      to: "Commit hashes from 14-01, 14-02, 14-03"
      via: "recorded in the close-out table as evidence"
      pattern: "14-01|14-02|14-03"
    - from: "v1.1-MILESTONE-AUDIT.md close-out section"
      to: "v1.2 deferred items list (Issue B — no sign-up UI)"
      via: "explicit deferral annotation"
      pattern: "v1.2|Issue B"
---

<objective>
Close Phase 14 by re-running the milestone audit as the close gate (the audit's own recommended "3-minute" verification loop), recording the residual deferred items, and marking v1.1 ready to ship. This plan is deliberately small — it's the close-out ceremony, not new engineering work.

Per the audit's closing paragraph (lines 285-287 of v1.1-MILESTONE-AUDIT.md):
> "Path to close: run the 3-minute live UAT. Outcome determines whether v1.1 is `gaps_found` (with a 1-line fix) or `passed` (with a note that static analysis missed a server-side mechanism)."

Tasks 14-01/02/03 shipped the 1-line fix + retro-VERIFICATION + spoiler-gallery gate. This plan runs the re-audit and documents the outcome.

Output: An addendum section appended to `.planning/v1.1-MILESTONE-AUDIT.md` capturing the Phase 14 close-out; an updated `.planning/STATE.md` reflecting Phase 14 completion + v1.1 milestone-ready state.

Requirements covered: ALL Phase 14 REQ-IDs (SYNC-03, AUTH-06, SCHEMA-01, SCHEMA-02, SCHEMA-03, MARKET-01, PERF-04) — because this plan is where the phase-level close-gate is attested. The upstream plans did the work; this plan confirms the work closed the audit gaps end-to-end.
</objective>

<context>
@.planning/v1.1-MILESTONE-AUDIT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/14-v1-1-audit-gap-closure/14-01-PLAN.md
@.planning/phases/14-v1-1-audit-gap-closure/14-02-PLAN.md
@.planning/phases/14-v1-1-audit-gap-closure/14-03-PLAN.md

<interfaces>
Close-out addendum template — appended AFTER the existing audit content, preserving the original for historical record.

**Expected STATE.md fields to update (Task 3):**
- `stopped_at`: reflects Phase 14 completion + v1.1 ready to close
- `last_updated`: today's timestamp
- `last_activity`: today's date
- `progress.completed_phases`: increment by 1
- `progress.completed_plans`: increment by 4
- Milestone table row: v1.1 shipping-ready

**The milestone audit command (Task 1):**

The executor instructs the user to run `/gsd:audit-milestone v1.1` in a fresh Claude Code conversation (so context is clean for the audit agent). The audit is a slash-command that another Claude instance runs and produces its own artifact. Plan 14-04 records the OUTCOME of that audit, not the audit itself.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Re-run /gsd:audit-milestone v1.1 + capture outcome</name>
  <files>.planning/v1.1-MILESTONE-AUDIT.md</files>
  <read_first>
    - .planning/v1.1-MILESTONE-AUDIT.md (the original audit — your baseline for comparison)
    - .planning/phases/14-v1-1-audit-gap-closure/14-01-PLAN.md (confirm Plan 14-01 completed)
    - .planning/phases/14-v1-1-audit-gap-closure/14-02-PLAN.md (confirm Plan 14-02 completed)
    - .planning/phases/14-v1-1-audit-gap-closure/14-03-PLAN.md (confirm Plan 14-03 completed)
  </read_first>
  <action>
  **This is a human-verify checkpoint — Claude waits for the user to re-run /gsd:audit-milestone v1.1 in a fresh conversation and paste back the outcome.**

  <what-built>
    Plans 14-01, 14-02, 14-03 shipped. Each landed:
    - 14-01: `payload.user_id = currentUserId` stamp in sync-engine.js + 4-test regression suite + live huxley UAT passed + Phase 11 HUMAN-UAT flipped to resolved (closes Issues A + SYNC-03 + AUTH-06).
    - 14-02: Retrospective `07-VERIFICATION.md` + SCHEMA-01..03 checkboxes flipped to [x] with dated annotations (closes Issue C + SCHEMA-01..03).
    - 14-03: 2-branch bulkdata gate on spoiler-gallery + 6-test regression suite (closes Issue D + MARKET-01 + PERF-04 honesty-gating completeness).

    Before Phase 14 closes, the audit must be re-run as the close gate. The audit agent uses static analysis + file-existence checks — Tasks 14-01/02/03 should have moved the audit from `gaps_found` to `passed` (or at minimum dropped Issues A, C, D from the critical/minor lists).

    Issue B (no sign-up UI) is intentionally NOT addressed by Phase 14 — it's a design decision deferred to v1.2 per the phase scope and the original audit's own recommendation (lines 266-269).
  </what-built>

  <how-to-verify>
    **Step 1 — Confirm Plans 14-01, 14-02, 14-03 all landed:**

    Run these commands and capture the outputs:

    - `grep -c "user_id: currentUserId" src/services/sync-engine.js` — expect ≥ 1 (14-01 fix landed)
    - `test -f tests/sync-push-userid.test.js` — expect exit 0 (14-01 test exists)
    - `grep -c "result: passed" .planning/phases/11-cloud-sync-engine/11-HUMAN-UAT.md` — expect ≥ 1 (14-01 live UAT annotation)
    - `test -f .planning/phases/07-polish-pass-perf-baseline-schema-migration/07-VERIFICATION.md` — expect exit 0 (14-02 verif exists)
    - `grep -c "^- \[x\] \*\*SCHEMA-0" .planning/REQUIREMENTS.md` — expect 3 (14-02 checkboxes flipped)
    - `grep -c "verified retroactively" .planning/REQUIREMENTS.md` — expect ≥ 3 (14-02 annotations)
    - `grep -c "Archive is downloading" src/components/spoiler-gallery.js` — expect 1 (14-03 loading copy)
    - `test -f tests/spoiler-gallery-bulkdata-gating.test.js` — expect exit 0 (14-03 test exists)
    - `npm test` — expect green (full suite passes)

    All evidence must be present before running the re-audit. If any evidence is missing, the relevant Plan (14-01 / 14-02 / 14-03) is incomplete — route back and fix before re-auditing.

    **Step 2 — Re-run the milestone audit:**

    In a fresh Claude Code conversation (or `/clear` the current context for a clean read), run: `/gsd:audit-milestone v1.1`

    The command will produce a new audit artifact (typically a new file OR an updated `.planning/v1.1-MILESTONE-AUDIT.md` — check the command's behaviour; if it overwrites the existing file, COPY the original to `.planning/v1.1-MILESTONE-AUDIT-2026-04-22.md` FIRST to preserve history).

    **Step 3 — Compare the new audit to the original:**

    Expected outcomes (what "passed" looks like):
    - New audit frontmatter: `status: passed` (was `gaps_found`)
    - `gaps.critical` is empty or absent (previously contained Issue A)
    - `gaps.minor` no longer contains Issue C or Issue D (may still contain Issue B — this is expected, Phase 14 deferred it)
    - `scores.requirements`: 48/48 checkbox-complete (was "45/45 + 3 stale-unchecked")
    - `scores.integration`: 7/7 cross-phase links verified (was 6/7)
    - Phase 7 now has `VERIFICATION.md` in the Phase Status table
    - Phase 11 HUMAN-UAT shows `resolved` (was `pending`)

    Minimum-acceptable outcomes (if full "passed" isn't achievable):
    - Issue A (critical) is RESOLVED in the new audit's gaps section
    - Issue C (docs) is RESOLVED in the new audit's gaps section
    - Issue D (minor UX) is RESOLVED in the new audit's gaps section
    - Issue B may remain (deferred — acceptable)
    - Nyquist coverage may remain partial (informational only — acceptable)
    - New minor issues may surface (document them; decide case-by-case whether to ship or patch)

    **Step 4 — Capture the outcome:**

    Report back with:
    1. The new audit's `status:` field value
    2. The list of any remaining `gaps.critical` or `gaps.minor` items
    3. Whether Issues A, C, D appear as resolved
    4. Path to the new audit artifact (or confirmation that the existing file was updated + the original was preserved as a dated copy)

    **Red-flag outcomes:**
    - Issue A still appears in `gaps.critical` → Task 14-01 Task 2 regressed or Task 3 UAT missed something — go back to 14-01
    - Issue C still appears → 14-02 is incomplete (VERIFICATION.md missing or REQUIREMENTS.md not flipped)
    - Issue D still appears → 14-03 is incomplete (spoiler-gallery.js not gated or test not landed)
    - New critical issues appear that weren't in the original audit → investigate; may need a follow-on plan
  </how-to-verify>

  <resume-signal>
    Type "passed" if the re-audit returned `status: passed` OR returned a status where Issues A, C, D are all marked resolved (even if Issue B or other deferred items remain).
    Type "partial: {details}" if Issues A, C, or D are still critical/minor in the re-audit — include the exact gap description so Task 2 can record it.
    Type "failed: {details}" if new critical issues surfaced that weren't in the original audit — Phase 14 doesn't close until these are addressed.
  </resume-signal>
  </action>
  <verify>
    <automated>grep -cE "status:\s*passed" .planning/v1.1-MILESTONE-AUDIT*.md 2>/dev/null || echo "awaiting user re-audit outcome"</automated>
  </verify>
  <done>Re-audit executed in a fresh conversation; user pasted the outcome (status: passed / partial / failed); original audit file preserved (either unchanged or copied to a dated filename before overwrite); Issues A, C, D confirmed dropped from critical/minor lists; Task 2 has the outcome string to embed in the close-out addendum.</done>
</task>

<task type="auto">
  <name>Task 2: Append Phase 14 close-out addendum to v1.1-MILESTONE-AUDIT.md</name>
  <files>.planning/v1.1-MILESTONE-AUDIT.md</files>
  <read_first>
    - .planning/v1.1-MILESTONE-AUDIT.md (the ORIGINAL file — Task 1 may have preserved it as a dated copy if the re-audit overwrote it; this task appends to whichever copy is the canonical resolution log)
    - The Task 1 outcome (resume-signal payload from Task 1 — must be available in context)
    - Commit log of 14-01, 14-02, 14-03: run `git log --oneline -20` to grab the 6-7 commit hashes landed by Plans 14-01 (3 commits: RED/GREEN/docs), 14-02 (2 commits), 14-03 (2 commits)
  </read_first>
  <action>
    Append a new section to the END of `.planning/v1.1-MILESTONE-AUDIT.md` (do NOT overwrite any existing content — this is an addendum, preserving the original audit as historical record). The section must appear AFTER the existing `## Milestone Status Determination` section (currently the last section).

    Use today's date in YYYY-MM-DD format wherever 2026-04-22 appears.

    **Addendum to append (exact structure — use verbatim heading + field names):**

    Start with a horizontal-rule separator (three dashes on their own line with blank lines above and below — standard markdown `---` used inside the body, not as frontmatter).

    Then the H2 heading: `## Phase 14 Close-Out — Gap Resolution Log`

    Then a 4-line intro block:
    - `**Closed:** 2026-04-22` (use today's date)
    - `**Closed by:** Phase 14 (.planning/phases/14-v1-1-audit-gap-closure/)`
    - `**Plans executed:** 14-01, 14-02, 14-03, 14-04`
    - `**Re-audit outcome:** {paste the Task 1 resume-signal payload verbatim — e.g., "passed — Issues A, C, D all resolved; Issue B deferred to v1.2"}`

    Then an H3 section `### Resolution Evidence` with a markdown table (columns: Issue, Original Severity, Resolution Plan, Commit(s), Evidence). Three rows:

    Row A: Issue=`A — sync push omits user_id`, Severity=`CRITICAL`, Plan=`14-01`, Commits=`{HASH-RED}, {HASH-GREEN}, {HASH-DOCS}` (substitute real hashes), Evidence=`src/services/sync-engine.js line ~410 stamps user_id: currentUserId; tests/sync-push-userid.test.js 4/4 green; 11-HUMAN-UAT.md live anchor result: passed`.

    Row C: Issue=`C — Phase 7 VERIFICATION missing`, Severity=`DOC`, Plan=`14-02`, Commits=`{HASH-VERIF}, {HASH-FLIP}`, Evidence=`.planning/phases/07-polish-pass-perf-baseline-schema-migration/07-VERIFICATION.md exists with status: passed-retrospective; REQUIREMENTS.md SCHEMA-01/02/03 flipped to [x] with retroactive-verified annotations`.

    Row D: Issue=`D — spoiler-gallery empty-state dishonest`, Severity=`MINOR UX`, Plan=`14-03`, Commits=`{HASH-RED-D}, {HASH-GREEN-D}`, Evidence=`src/components/spoiler-gallery.js applies $store.bulkdata?.status === 'ready' gate with "Archive is downloading…" loading copy; tests/spoiler-gallery-bulkdata-gating.test.js 6/6 green`.

    Then an H3 section `### Deferred Items` with a markdown table (columns: Issue, Original Severity, Deferral Target, Reason). Three rows:

    - Issue=`B — no sign-up UI surface`, Severity=`MINOR`, Target=`v1.2`, Reason=`Design decision — household/private-collaborator model may be intentional per 10-CONTEXT.md line 113; confirm with product intent before adding a public sign-up flow`
    - Issue=`Nyquist coverage (0 compliant, 6 partial, 2 missing)`, Severity=`INFO`, Target=`v1.2 process pass`, Reason=`Informational-only per audit — does not block milestone; addressed via backfilling VALIDATION.md across 8 phases or explicitly disabling Nyquist via config`
    - Issue=`13-FINDINGS.md Preordain "Upcoming Releases shows 1993 sets"`, Severity=`MINOR`, Target=`v1.2 or dedicated bug-fix plan`, Reason=`Pre-existing, out-of-scope for v1.1 per Phase 13 Plan 3 triage`

    Then an H3 section `### Milestone Status` with two paragraphs:

    Para 1: `**v1.1 Second Sunrise — READY TO SHIP.** All critical + documentation gaps closed. Minor UX gap closed. Deferred items recorded and scoped to v1.2.`

    Para 2: `**Cloud sync push verified live** against huxley Supabase (Phase 14 Plan 01 Task 3): sign-in + add-card round-trip confirmed counterflux.collection upsert with correct user_id, sync chip stayed synced, sync_conflicts empty — the audit's "latent but provable wiring break" is resolved.`

    Substitute the `{HASH-*}` placeholders with real commit hashes from `git log --oneline -20`. If you can't identify which commit is which from the log, use full short-hash + message (e.g., `abc1234 (test(14-01): RED — assert payload.user_id stamped)`) — the goal is traceability, not format purity.

    Substitute the `{Task 1 resume-signal payload}` placeholder with the actual resume-signal payload from Task 1.

    Do NOT modify the YAML frontmatter at the top of the file. Do NOT modify the original `## Executive Summary`, `## Phase Status`, `## Requirements Coverage`, `## Cross-Phase Integration Findings`, `## Critical Gap: Issue A`, `## Tech Debt Summary`, `## Nyquist Coverage`, `## Recommended Actions`, or `## Milestone Status Determination` sections. The addendum is strictly additive.

    Commit with message `docs(14-04): append Phase 14 close-out addendum to v1.1 milestone audit`.
  </action>
  <acceptance_criteria>
    - `grep -c "## Phase 14 Close-Out" .planning/v1.1-MILESTONE-AUDIT.md` returns exactly 1
    - `grep -c "Plans executed: 14-01, 14-02, 14-03" .planning/v1.1-MILESTONE-AUDIT.md` returns 1 (or equivalent — the addendum names the plans)
    - `grep -c "### Resolution Evidence" .planning/v1.1-MILESTONE-AUDIT.md` returns 1
    - `grep -c "### Deferred Items" .planning/v1.1-MILESTONE-AUDIT.md` returns 1
    - `grep -c "### Milestone Status" .planning/v1.1-MILESTONE-AUDIT.md` returns 1
    - The string `Issue B` appears at least twice in the file (once in original audit, once in Deferred Items section with v1.2 in the same row)
    - The addendum appears AFTER the existing `## Milestone Status Determination` section — confirm with `grep -n "## Milestone Status Determination\|## Phase 14 Close-Out" .planning/v1.1-MILESTONE-AUDIT.md` (the close-out line number must be greater)
    - The original `## Executive Summary` heading is still present — nothing was deleted
    - Git log shows a commit with message matching `docs(14-04): append Phase 14 close-out`
  </acceptance_criteria>
  <verify>
    <automated>grep -c "## Phase 14 Close-Out" .planning/v1.1-MILESTONE-AUDIT.md && grep -c "### Resolution Evidence" .planning/v1.1-MILESTONE-AUDIT.md && grep -c "### Deferred Items" .planning/v1.1-MILESTONE-AUDIT.md</automated>
  </verify>
  <done>Close-out addendum appended with commit hashes, resolution evidence table, deferred items table, and milestone status confirmation; original audit content preserved verbatim; commit landed.</done>
</task>

<task type="auto">
  <name>Task 3: Update STATE.md to reflect Phase 14 completion + v1.1 ready-to-close</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md (current state — you're editing frontmatter + Current Position + Milestone Progress + Pending Todos)
    - .planning/ROADMAP.md (to confirm Phase 14's plan count is 4 for the completed_plans bump)
    - The Task 1 outcome (for the close-out summary)
  </read_first>
  <action>
    Open `.planning/STATE.md`. Update the following fields (do NOT restructure the file — in-place edits only):

    **1. Frontmatter (lines 1-14):**

    - Change `stopped_at:` to: `"Phase 14 (v1.1 audit gap closure) complete — Issues A, C, D resolved; v1.1 Second Sunrise READY TO SHIP (Issue B deferred to v1.2)"`
    - Update `last_updated:` to today's ISO timestamp (same format as existing — e.g., `"2026-04-22T[HH:MM:SS].000Z"`)
    - Update `last_activity:` to today's date (YYYY-MM-DD)
    - In `progress:` block:
      - Bump `completed_phases` by 1 (was 8, becomes 9 — Phase 14 counts as a phase per ROADMAP table)
      - Bump `completed_plans` by 4 (Phase 14 shipped 4 plans per this phase's scope)
      - Recompute `percent`: with v1.1 ready to ship, this is a fresh snapshot — aim for a number that reflects reality. If total_plans becomes 35 + 4 = 39 and completed_plans = 35 + 4 = 39, percent = 100 for v1.1. Check total_phases + total_plans denominators before committing a value.

    **2. "Current Position" section (lines ~26-33):**

    Replace the 4 lines:

    ```
    Phase: 13
    Plan: Not started
    Status: Ready to execute
    Last activity: 2026-04-22
    ```

    With:

    ```
    Phase: 14
    Plan: Complete (all 4 plans shipped)
    Status: v1.1 milestone ready to ship
    Last activity: {today's date}
    ```

    Update the progress bar line (currently `Progress: [████████░░] 84%`) to reflect the new percent — use a bar matching the new value (e.g., 100% = `[██████████]`).

    **3. "Milestone Progress" table (around line 37-40):**

    Update the v1.1 row to reflect shipping-ready status:

    `| v1.1 Second Sunrise | 9/9 | Ready to ship — Phase 14 gap closure complete |`

    Adjust the phase count 9/9 to match actual phase count in ROADMAP — at time of writing, v1.1 shows "7/8 phases" in audit + Phase 14 added = 9 phase entries; verify against `.planning/ROADMAP.md` Progress table.

    **4. "Pending Todos" section (around line 211-213):**

    Replace the current content (`None — roadmap complete, next step is /gsd:plan-phase 7.`) with:

    `None — Phase 14 complete, v1.1 Second Sunrise ready to ship. Next step: ship v1.1 + begin v1.2 scoping. Deferred items for v1.2: Issue B (sign-up UI), Nyquist backfill, 13-FINDINGS Preordain bug.`

    **5. "Session Continuity" section (last 3 lines of file):**

    Replace with:

    ```
    Last session: {today's ISO timestamp}
    Stopped at: Phase 14 complete — v1.1 ready to ship. Issues A, C, D resolved per .planning/v1.1-MILESTONE-AUDIT.md close-out addendum.
    Resume file: None
    ```

    Do NOT touch the "Performance Metrics" velocity table or the "Accumulated Context → Decisions" history — both are historical logs, not live state.

    Commit with message `docs(14-04): update STATE.md — Phase 14 complete, v1.1 ready to ship`.
  </action>
  <acceptance_criteria>
    - `grep -c "Phase 14" .planning/STATE.md` returns at least 3 matches (frontmatter, Current Position, Pending Todos)
    - `grep -c "v1.1 milestone ready to ship\|v1\.1.*[Rr]eady to ship" .planning/STATE.md` returns at least 2 matches
    - `grep -c "Issue B" .planning/STATE.md` returns at least 1 (deferred item record in Pending Todos)
    - The frontmatter `progress.completed_phases` value increased by 1 vs pre-edit state
    - The frontmatter `progress.completed_plans` value increased by 4 vs pre-edit state (for the 4 Phase 14 plans)
    - The `Current Position` section shows `Phase: 14` (was `Phase: 13`)
    - The Milestone Progress table v1.1 row shows updated phase count with "Ready to ship" (or equivalent) status text
    - Git log shows a commit with message matching `docs(14-04): update STATE.md`
  </acceptance_criteria>
  <verify>
    <automated>grep -c "Phase 14" .planning/STATE.md && grep -cE "v1\.1.*(R|r)eady to ship" .planning/STATE.md && grep -c "Issue B" .planning/STATE.md</automated>
  </verify>
  <done>STATE.md frontmatter + Current Position + Milestone Progress + Pending Todos + Session Continuity all reflect Phase 14 completion and v1.1 ready-to-ship status; commit landed.</done>
</task>

</tasks>

<verification>
Plan 14-04 is complete when ALL of these are true:

1. Task 1 human-verify checkpoint returned "passed" OR "partial" with Issues A, C, D confirmed resolved
2. `grep -c "## Phase 14 Close-Out" .planning/v1.1-MILESTONE-AUDIT.md` returns 1
3. `grep -c "Issue B" .planning/v1.1-MILESTONE-AUDIT.md` returns at least 2 (original Issue B section + the deferred-items addendum row)
4. `grep -c "Phase 14" .planning/STATE.md` returns at least 3
5. `grep -cE "v1\.1.*(R|r)eady to ship" .planning/STATE.md` returns at least 2
6. Git log contains 2 commits from this plan: close-out addendum + STATE.md update
</verification>

<success_criteria>
- The re-audit close gate has been run against the canonical v1.1 milestone. Its outcome is recorded verbatim in the addendum.
- Original audit document is PRESERVED (not overwritten) — historical record of the 2026-04-22 findings remains intact, addendum shows the resolution.
- STATE.md reflects the new reality: Phase 14 done, v1.1 ready to ship, deferred items named with their target milestone.
- Any residual gaps (new surprises from the re-audit, Issue B reasons for deferral) are captured in the close-out addendum so future milestone planners have context.
- The milestone is closeable — user can now tag v1.1 / merge to main / cut a release / whatever their shipping ritual is, with zero ambiguity about what's in v1.1 and what's deferred.
</success_criteria>

<output>
After completion, the orchestrator will create `.planning/phases/14-v1-1-audit-gap-closure/14-04-SUMMARY.md` capturing:
- Re-audit outcome + artifact path
- Close-out addendum structure + commit hash
- STATE.md delta + commit hash
- v1.1 shipping-readiness attestation
</output>
