---
status: partial
phase: 08.1-treasure-cruise-polish-precon-coverage
source: [08.1-VERIFICATION.md]
started: 2026-04-16T15:00:00Z
updated: 2026-04-16T15:00:00Z
---

## Current Test

[awaiting human testing]

## Tests

### 1. FOLLOWUP-1 — Dropdown scroll containment in browser
expected: Dropdown scroll is self-contained — all 8 'lightning' search results are reachable; the parent `<aside>` never clips the dropdown. The dropdown's scrollbar lives on the dropdown wrapper, not the panel.
steps: `npm run dev` → /treasure-cruise → type 'lightning' in LHS panel search → confirm scrollbar inside dropdown bounds, all 8 rows reachable, panel itself doesn't show its own scrollbar.
result: [pending]

### 2. FOLLOWUP-2 — Re-open affordance visual prominence
expected: Affordance is unmistakable — 48px tall hit-target, primary blue (#0D52BD) background with glow-blue box-shadow, monospace 'OPEN PANEL' label paired with chevron_right glyph. Hover increases box-shadow blur. Click re-opens the panel.
steps: `npm run dev` → /treasure-cruise → click chevron_left to collapse panel → inspect top-left of grid where re-open button appears.
result: [pending]

### 3. FOLLOWUP-3 — Hover-checkbox affordance + keyboard accessibility
expected: Hover reveals checkbox; click/Enter/Space all open the same context menu (EDIT QUANTITY / TOGGLE FOIL / MOVE TO WISHLIST / REMOVE / etc.); right-click still works; reduced-motion disables the opacity transition.
steps: Seed ≥10 cards → /treasure-cruise gallery view → hover any tile (kebab `more_vert` glyph fades in top-right) → click checkbox (same menu as right-click) → focus tile via Tab (checkbox visible while focused) → press Enter (menu opens at tile centre) → press Space (menu opens) → right-click (existing flow still works) → if 200+ collection: confirm virtual-scroller path works → enable OS reduced-motion (opacity transition becomes instant).
result: [pending]

### 4. FOLLOWUP-4A — BROWSE PRECONS surfaces newly-allowlisted Commander products
expected: All 18 allowlisted Commander products appear when Scryfall returns them; non-allowlisted neighbours (mb2, 2x2, dmr) stay filtered out.
steps: `npm run dev` → /treasure-cruise → click BROWSE PRECONS (clear localStorage or click REFRESH to bust cache) → confirm new tiles render: Commander Masters (cmm), Commander Legends BFBG (clb), Planechase Anthology (pca), Archenemy (arc), Premium Deck Series Slivers (h09), Game Night (gnt), etc. → search for `mb2` Mystery Booster 2 (negative control: same set_type as cmm but not allowlisted; should NOT appear).
result: [pending]

### 5. FOLLOWUP-4B — Multi-deck bundle warning + ADD ALL disabled
expected: Bundle warning replaces the 1000-card list; ADD ALL is greyed; clicking it is a silent no-op (no toast, no Dexie write, no undo entry); browser stays open so warning copy stays visible.
steps: Click `cmm` Commander Masters tile (~1067 cards) → confirm warning state renders: yellow MULTI-DECK PRODUCT heading + 'This product contains multiple decks — open in Scryfall to pick a specific deck.' body + cards-count subline + OPEN IN SCRYFALL link → confirm ADD ALL is greyed/disabled with label 'MULTI-DECK PRODUCT' (not 'ADD ALL 1067 CARDS') → click disabled ADD ALL (silent no-op: collection count unchanged, no toast, no console error) → click any single-deck Commander product (~100 cards) — confirm decklist preview renders normally with active ADD ALL button.
result: [pending]

## Summary

total: 5
passed: 0
issues: 0
pending: 5
skipped: 0
blocked: 0

## Gaps
