---
phase: 08.1-treasure-cruise-polish-precon-coverage
verified: 2026-04-16T14:50:00Z
status: human_needed
score: 5/5 success criteria met at code level; 3/5 require browser-based human UAT for visual/motion confirmation
re_verification: false
human_verification:
  - test: "FOLLOWUP-1 — Dropdown scroll containment in browser"
    steps:
      - "npm run dev, navigate to /treasure-cruise"
      - "Type 'lightning' in the LHS panel search box (returns 8 results)"
      - "Confirm the dropdown shows a scrollbar INSIDE the dropdown bounds"
      - "Confirm scrolling reveals all 8 rows without clipping by the panel boundary"
      - "Confirm the panel itself does NOT show its own scrollbar reacting to the dropdown content"
    expected: "Dropdown scroll is self-contained — all 8 search results are reachable; the parent <aside> never clips the dropdown. The dropdown's scrollbar lives on the dropdown wrapper, not the panel."
    why_human: "Visual scroll containment behaviour requires real DOM rendering + manual scroll interaction; cannot be programmatically verified beyond CSS attribute presence."
  - test: "FOLLOWUP-2 — Re-open affordance visual prominence"
    steps:
      - "npm run dev, navigate to /treasure-cruise"
      - "Click the chevron_left in the LHS panel header to collapse the panel"
      - "Inspect the top-left of the grid where the re-open button now appears"
    expected: "Affordance is unmistakable: 48px tall hit-target, primary blue (#0D52BD) background with glow-blue box-shadow, monospace 'OPEN PANEL' label paired with chevron_right glyph. Hover increases box-shadow blur. Click re-opens the panel."
    why_human: "Visibility/prominence is a human perception judgement — the user found the original 32px ghost-bordered version too subtle; only a sighted human can confirm the new treatment now passes the 'I can see this' bar."
  - test: "FOLLOWUP-3 — Hover-checkbox affordance + keyboard accessibility"
    steps:
      - "Seed at least 10 cards in the collection"
      - "Navigate to /treasure-cruise gallery view"
      - "Hover over any card tile — confirm a small primary-blue button (kebab `more_vert` glyph) fades in at the top-right of the tile image area"
      - "Click the checkbox — confirm the same context menu opens that right-click already opens"
      - "Move focus away — confirm the checkbox fades back to opacity 0"
      - "Tab into the gallery — confirm focus indicator lands on a card tile and the checkbox becomes visible while the tile has focus"
      - "With a tile focused, press Enter — confirm context menu opens at the centre of the tile"
      - "With a tile focused, press Space — confirm context menu opens"
      - "Right-click a tile — confirm the existing right-click flow still works"
      - "If a 200+ card collection is available, confirm hover-checkbox works in the virtual-scroller path"
      - "Enable OS reduced-motion and confirm the opacity transition becomes instant"
    expected: "Hover reveals checkbox; click/Enter/Space all open the same context menu (EDIT QUANTITY / TOGGLE FOIL / MOVE TO WISHLIST / REMOVE / etc.); right-click still works; reduced-motion disables the opacity transition."
    why_human: "Hover reveal, focus state, keyboard navigation, and motion preference all require live browser interaction. Test E1-E5 in tests/gallery-view.checkbox.test.js confirm the markup + CSS contract; only browser UAT confirms the perceptual experience."
  - test: "FOLLOWUP-4A — BROWSE PRECONS surfaces newly-allowlisted Commander products"
    steps:
      - "npm run dev, navigate to /treasure-cruise"
      - "Click BROWSE PRECONS (clear localStorage or click REFRESH to bust the precons cache)"
      - "Confirm new tiles render: Commander Masters (cmm), Commander Legends Battle for Baldur's Gate (clb), Planechase Anthology (pca), Archenemy (arc), Premium Deck Series Slivers (h09), Game Night (gnt), etc."
      - "Search for `mb2` Mystery Booster 2 — confirm it does NOT appear (negative control: same set_type as cmm but not allowlisted)"
    expected: "All 18 allowlisted Commander products appear when Scryfall returns them; non-allowlisted neighbours (mb2, 2x2, dmr) stay filtered out."
    why_human: "Live Scryfall API call returns the real /sets payload; only browser-driven walkthrough confirms the user-visible tile grid matches the allowlist contract. Test A1-A5 in tests/precons.test.js verify the filter logic against fixtures."
  - test: "FOLLOWUP-4B — Multi-deck bundle warning + ADD ALL disabled"
    steps:
      - "Click `cmm` (Commander Masters, ~1067 cards) tile in BROWSE PRECONS"
      - "Confirm the warning state renders: yellow MULTI-DECK PRODUCT heading + 'This product contains multiple decks — open in Scryfall to pick a specific deck.' body + cards-count subline + OPEN IN SCRYFALL link"
      - "Confirm the ADD ALL button is greyed/disabled with label 'MULTI-DECK PRODUCT' (not 'ADD ALL 1067 CARDS')"
      - "Click the disabled ADD ALL button — confirm true no-op (collection topbar count unchanged, no toast appears, no console error)"
      - "Click any single-deck Commander product (~100 cards) — confirm decklist preview renders normally with active ADD ALL button"
    expected: "Bundle warning replaces the 1000-card list; ADD ALL is greyed; clicking it is a silent no-op (no toast, no Dexie write, no undo entry); browser stays open so the warning copy stays visible."
    why_human: "Live Scryfall API call returns real bundle decklists; only browser walkthrough confirms the warning surface fires and ADD ALL is true no-op end-to-end. Test C1-C5 + render test in tests/collection.precon.test.js verify the store + render contract against fixtures."
---

# Phase 08.1: Treasure Cruise Polish & Precon Coverage Verification Report

**Phase Goal:** Resolve four user-reported polish/bugfix items captured during Phase 8 human-UAT walkthrough — dropdown scroll cutoff, collapsed-panel re-open affordance, collection-grid hover-checkbox edit, and Commander precon coverage gap (allowlist widening + multi-deck size guard).

**Verified:** 2026-04-16T14:50:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (Success Criteria from ROADMAP)

| #   | Truth | Status | Evidence |
| --- | ----- | ------ | -------- |
| 1 | LHS add-card panel dropdown shows all matching results within an internally-scrollable area; no clipping by panel boundary; scrollbar lives on the dropdown, not the panel | VERIFIED (code) / NEEDS HUMAN (visual) | `src/components/add-card-panel.js:140` (aside `overflow: visible`) + `:145` (`.tc-panel-body` inner wrapper with `flex: 1; min-height: 0; overflow-y: auto`) + `:206` (dropdown `max-height: 280px; overflow-y: auto; position: absolute`). Test file `tests/add-card-panel.dropdown.test.js` includes 4 new FOLLOWUP-1 assertions, all pass. |
| 2 | Collapsed-panel re-open affordance is unmistakable — visually obvious chevron_right button (brighter accent, larger, or paired label) at top-left of collection grid | VERIFIED (code) / NEEDS HUMAN (visual prominence) | `src/screens/treasure-cruise.js:45-54` (button now `class="cf-panel-reopen"` with paired `<span>OPEN PANEL</span>` label). `src/styles/main.css:183-216` (`.cf-panel-reopen` utility — 48px height, `var(--color-primary)` background, glow-blue box-shadow, JetBrains Mono 11/700 uppercase, chevron_right at 24px). Hover/focus-visible variants present. |
| 3 | Collection grid card tiles expose a checkbox on hover that opens the same quick-action menu as right-click; keyboard-accessible | VERIFIED (code) / NEEDS HUMAN (hover/focus motion + keyboard interaction) | `src/styles/main.css:227-260` (`.card-quick-actions-checkbox` opacity 0 default → opacity 1 on parent `:hover` / `:focus-within` / self `:focus`). `src/components/card-tile.js:34,57-67` (safeName escape + checkbox button with `@click.stop` + `@keydown.enter.stop.prevent` + `@keydown.space.stop.prevent` dispatching `card-context-menu`). `src/components/gallery-view.js:39-50` (direct path) + `:110, 134-150` (virtual-scroller path with delegated click + keydown listeners using data-entry-id lookup against `store.entries`). `tabindex="0"` on tile root in both paths. `src/components/context-menu.js:179` (existing `card-context-menu` listener handles all sources identically — no new listener added). |
| 4 | BROWSE PRECONS surfaces previously-missing Commander products via code-level allowlist (18 codes: cmm, clb, cmr, cm1, cc1, cc2, pd2, pd3, h09, pca, pc2, hop, arc, e01, pltc, gnt, gn2, gn3) | VERIFIED (code) / NEEDS HUMAN (live Scryfall walkthrough) | `src/services/precons.js:42-52` (`PRECON_EXTRA_CODES` exported, exactly 18 entries — runtime verification: `count: 18, missing: [], extra: []`). `src/services/precons.js:122` (`fetchPrecons` filter: `PRECON_SET_TYPES.includes(s.set_type) || PRECON_EXTRA_CODES.includes(s.code)` — disjunctive). 5 FOLLOWUP-4A tests pass (A1 cmm via allowlist, A2 clb/pca/arc/pltc, A3 mb2 negative control, A4 total count, A5 export shape). |
| 5 | Multi-deck Commander products (decklist.length > 200) display "open in Scryfall" message instead of dumping 400-1000 card list; ADD ALL is disabled for bundled products | VERIFIED (code) / NEEDS HUMAN (live bundled-precon walkthrough) | `src/services/precons.js:66` (`MULTI_DECK_BUNDLE_THRESHOLD = 200`) + `:83-86` (`isMultiDeckBundle` defensive helper). Runtime verified: `isMultiDeckBundle(199): false, (200): false, (201): true` — strict `> 200` per plan. `src/components/precon-browser.js:191` (`isBundle` getter), `:215` (ADD ALL `:disabled` includes `isBundle`), `:220` (label flips to `MULTI-DECK PRODUCT`), `:241-257` (warning template with verbatim copy "This product contains multiple decks — open in Scryfall to pick a specific deck."). `src/stores/collection.js:411-413` (`addAllFromPrecon` early-return guard between no-decklist throw and Dexie transaction — zero side effects). 6 FOLLOWUP-4B tests pass (3 helper + 5 store guard + 1 render). |

**Score:** 5/5 success criteria verified at code level. 5/5 also require human UAT for visual/motion/live-API confirmation per the engaged user's UAT-walkthrough commitment.

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `src/components/add-card-panel.js` | aside `overflow: visible` + `.tc-panel-body` wrapper with own scroll + dropdown self-scroll preserved | VERIFIED | Lines 140 (aside style), 145 (`.tc-panel-body`), 206 (dropdown attrs). |
| `src/screens/treasure-cruise.js` | re-open button `class="cf-panel-reopen"` + inline `OPEN PANEL` label + chevron_right glyph | VERIFIED | Lines 45-54. |
| `src/styles/main.css` | `.cf-panel-reopen` utility + `.card-quick-actions-checkbox` utility + single merged `@media (prefers-reduced-motion: reduce)` block | VERIFIED | `.cf-panel-reopen` at 183-216; `.card-quick-actions-checkbox` at 227-260; `@media` block at 155-163 contains all four selectors (`tc-panel-column, tc-grid-column, cf-panel-reopen, card-quick-actions-checkbox`) — single block, no duplicates. |
| `src/services/precons.js` | `PRECON_EXTRA_CODES` (length 18) + `isMultiDeckBundle` helper + `fetchPrecons` filter widened disjunctively | VERIFIED | 42-52 (allowlist), 83-86 (helper), 122 (filter). |
| `src/components/precon-browser.js` | `isBundle` getter + ADD ALL disabled when bundled + multi-deck warning template + decklist guard extended | VERIFIED | 28 (import), 61-63 (window bridge), 191 (getter), 215-220 (ADD ALL bundle handling), 241-257 (warning template), 260 (decklist `&& !isBundle`). |
| `src/stores/collection.js` | `addAllFromPrecon` early-return guard before Dexie transaction | VERIFIED | 5 (import), 411-413 (guard before line 415 `nowIso` / 420 transaction). |
| `src/components/gallery-view.js` | hover-checkbox in BOTH render paths + keyboard handlers + delegated listener for virtual-scroller | VERIFIED | 19-71 (direct path with Alpine bindings), 76-159 (virtual-scroller path with `tabindex`, `data-entry-id`, checkbox markup in renderItem string, delegated click + keydown listeners attached after `createVirtualScroller`, cleanup in `destroy()`). |
| `src/components/card-tile.js` | hover-checkbox + tabindex + safeName escape + Enter/Space handlers | VERIFIED | 34 (safeName), 37-44 (tile root with tabindex + keydown handlers), 57-67 (checkbox button). |
| `src/components/context-menu.js` | existing `card-context-menu` listener unchanged | VERIFIED | Line 179 — single document-level listener, source-agnostic. No new listener added. |
| `tests/gallery-view.checkbox.test.js` | new test file with 5 assertions covering hover-checkbox affordance | VERIFIED | File exists; 5 tests pass. |
| `tests/add-card-panel.dropdown.test.js` | 4 new FOLLOWUP-1 assertions for dropdown scroll containment | VERIFIED | File exists; runs in passing test suite. |

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | -- | --- | ------ | ------- |
| `add-card-panel.js` aside | dropdown wrapper | `overflow: visible` on aside + `position: absolute` + `max-height: 280px` + `overflow-y: auto` on dropdown | WIRED | Aside at 140 declares `overflow: visible`; dropdown at 206 has all three attrs in same style attribute. |
| `treasure-cruise.js` re-open button | `.cf-panel-reopen` utility | `class="cf-panel-reopen"` + `x-show="!$store.collection.panelOpen"` | WIRED | Button (45-54) is class-only consumer; no inline styles. |
| `precons.js fetchPrecons` | Scryfall /sets response filter | disjunctive predicate `PRECON_SET_TYPES.includes \|\| PRECON_EXTRA_CODES.includes` | WIRED | Line 122. |
| `precon-browser.js` VIEW B | ADD ALL button | `isBundle` getter feeds `:disabled` + `:title` + `:style` + `x-text` | WIRED | 191 (getter), 215-220 (4 reactive bindings). |
| `collection.js addAllFromPrecon` | Dexie transaction | `if (isMultiDeckBundle(precon)) return;` BEFORE `nowIso` and `db.transaction(...)` | WIRED | Lines 411-413 sit between the no-decklist throw (395) and `nowIso` (415) / transaction (420). |
| `card-tile.js` checkbox | `context-menu.js` | `$dispatch('card-context-menu', { entry, x, y })` (same payload as right-click) | WIRED | Lines 62-64; consumed by context-menu.js:179. |
| `gallery-view.js` virtual-scroller renderItem | card-tile markup | static HTML string includes `tabindex`, `data-entry-id`, checkbox markup; delegated click + keydown listeners on scroller container use `store.entries.find(id)` lookup | WIRED | renderItem 93-118; delegated handlers 134-150. |
| `main.css .card-quick-actions-checkbox` | `.card-tile-hover` (parent) | `:hover, :focus-within` on parent reveals child via opacity 0 → 1 | WIRED | 244-248. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| -------- | ------------- | ------ | ------------------ | ------ |
| `add-card-panel.js` dropdown | `searchResults` | `await window.__cf_searchCards(q, 8)` (line 55) → `searchCards` from `src/db/search.js` | Yes — local IndexedDB search via Dexie, paper-only Scryfall card data | FLOWING |
| `precon-browser.js` VIEW A tile grid | `$store.collection.precons` | `fetchPrecons()` from `src/services/precons.js` (cache-first via `db.precons_cache`, falls back to `queueScryfallRequest('https://api.scryfall.com/sets')`) | Yes — real Scryfall /sets payload, filtered through `PRECON_SET_TYPES \|\| PRECON_EXTRA_CODES` | FLOWING |
| `precon-browser.js` VIEW B `isBundle` | `precon` from `$store.collection.precons.find(p => p.code === selectedPreconCode)` | Same as above + `fetchPreconDecklist(code)` paginates `search_uri` (line 172-200 in precons.js) | Yes — `isBundle` reads `precon.decklist.length` against the live decklist | FLOWING |
| `gallery-view.js` tile grid (both paths) | `entry` | `$store.collection.sorted` getter, sourced from `store.entries` populated by `loadEntries()` (Dexie `db.collection.toArray()`) | Yes — real user collection rows | FLOWING |
| `treasure-cruise.js` re-open button visibility | `$store.collection.panelOpen` | `localStorage tc_panel_open` (D-03 / Pitfall 6) toggled by `togglePanel()` | Yes — persistent panel state | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| `PRECON_EXTRA_CODES` exported with exactly 18 entries matching ROADMAP success criterion #4 | `node -e "import('./src/services/precons.js').then(m => console.log(m.PRECON_EXTRA_CODES.length, ...))"` | count: 18, missing: [], extra: [] | PASS |
| `isMultiDeckBundle` strict-greater-than 200 threshold | runtime invocation with arrays of length 199 / 200 / 201 | 199 → false, 200 → false, 201 → true | PASS |
| All 4 plan-test files pass | `npx vitest run tests/add-card-panel.dropdown.test.js tests/gallery-view.checkbox.test.js tests/precons.test.js tests/collection.precon.test.js` | 4 files / 40 tests / 0 failed | PASS |
| Full suite passes minus the documented pre-existing vandalblast failure | `npx vitest run` | 1 failed / 591 passed / 10 todo (602 total). The single failure is `tests/router.test.js > Screen content > vandalblast shows game tracker screen with setup` — listed in `deferred-items.md` as pre-existing. | PASS (matches orchestrator pre-check) |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| ----------- | ---------- | ----------- | ------ | -------- |
| FOLLOWUP-1 | 08.1-01 | Dropdown scroll containment | SATISFIED | aside `overflow: visible` + `.tc-panel-body` inner scroll + dropdown self-scroll attrs preserved (`add-card-panel.js:140,145,206`); 4 new tests pass |
| FOLLOWUP-2 | 08.1-01 | Re-open affordance unmistakable | SATISFIED | `.cf-panel-reopen` 48px primary-accent utility + `OPEN PANEL` label paired with chevron_right (`treasure-cruise.js:45-54`, `main.css:183-216`) |
| FOLLOWUP-3 | 08.1-03 | Hover-checkbox + keyboard accessibility | SATISFIED | `.card-quick-actions-checkbox` utility + Alpine bindings in direct path + delegated listeners in virtual-scroller path + `tabindex="0"` + `card-context-menu` event reuse; 5 new tests pass |
| FOLLOWUP-4A | 08.1-02 | PRECON_EXTRA_CODES allowlist | SATISFIED | 18-code array exported from `precons.js:42-52`; `fetchPrecons` filter widened at line 122; 5 tests (A1-A5) pass; runtime confirms exact 18-code match |
| FOLLOWUP-4B | 08.1-02 | Multi-deck bundle guard | SATISFIED | `MULTI_DECK_BUNDLE_THRESHOLD = 200` + `isMultiDeckBundle` helper + `addAllFromPrecon` early-return + ADD ALL disable + warning template; 6 tests (3 helper + 5 store + 1 render) pass; runtime confirms strict `> 200` |

No orphaned requirements detected. All requirement IDs declared in the three plan frontmatters are accounted for.

### Cross-Plan Coordination Spot-Check

**NOTE 2 from prior plan-check** — `src/styles/main.css` should contain ONE `@media (prefers-reduced-motion: reduce)` block covering all four selectors:

VERIFIED. The block at `main.css:155-163` is the SOLE `@media (prefers-reduced-motion: reduce)` declaration in the file (grep confirms one match). Selector list:

```css
@media (prefers-reduced-motion: reduce) {
  .tc-panel-column,
  .tc-grid-column,
  .cf-panel-reopen,
  .card-quick-actions-checkbox {
    transition: none !important;
    transition-duration: 0.01ms !important;
  }
}
```

All four selectors present in a single block. No duplicates. Plan 01's commit `457b9d7` extended the existing Phase 8 Plan 2 block to add `.cf-panel-reopen`; Plan 03 (commit attribution shared per `08.1-03-SUMMARY.md` Deviations §1) further extended the same block to add `.card-quick-actions-checkbox`. The cross-plan @media coordination clause resolved cleanly.

### Out-of-Scope Guards

| Guard | Status | Evidence |
| ----- | ------ | -------- |
| Tier C: `src/data/precon-decks.js` curated deck-membership map MUST NOT exist | VERIFIED ABSENT | `D:/Vibe Coding/counterflux/src/data/` directory does not exist; no `precon-decks*` file found anywhere in `src/`. Deferred to Phase 08.2 per debug-file recommendation (also noted in `08.1-02-SUMMARY.md` Next Phase Readiness). |
| Item 5: browse-by-set + faceted filtering MUST NOT be implemented | VERIFIED ABSENT | No `browse*set*` file exists in `src/`. |

### Anti-Patterns Found

None blocking. All scanned files use real data flows; no stubs, no `TODO`/`FIXME`/`PLACEHOLDER` comments introduced; no `return null` or empty-array hardcodes in the modified rendering paths; the `addAllFromPrecon` early-return is intentional contract (silent no-op per Test C2/C3/C4) not a stub.

### SUMMARY Spot-Check vs. Codebase

| SUMMARY Claim | Codebase Reality | Status |
| ------------- | ---------------- | ------ |
| 08.1-01: aside switched from `overflow-y: auto` to `overflow: visible` + `.tc-panel-body` inner wrapper added | `add-card-panel.js:140` confirms `overflow: visible`; `:145` confirms `.tc-panel-body` with `flex: 1; min-height: 0; overflow-y: auto; padding: 24px` | MATCH |
| 08.1-01: re-open button is class-only consumer with `OPEN PANEL` label | `treasure-cruise.js:50-53` confirms `class="cf-panel-reopen"` + `<span>OPEN PANEL</span>` + chevron_right glyph; no inline width/height/background/onmouseenter | MATCH |
| 08.1-02: cmm fixture reclassified set_type 'commander' → 'masters' in place; 4 new allowlist rows + 1 negative-control row added; mockBundlePages exported | Plan-test verification shows fetchPrecons returns 9 rows (matching plan's expected baseline-4 + allowlist-5 = 9 with mb2 filtered out); test A3 (mb2 NOT in result) passes | MATCH |
| 08.1-02: addAllFromPrecon early-returns silently — no writes, no toast, no undo, no loadEntries | `collection.js:411-413` `if (isMultiDeckBundle(precon)) return;` sits BEFORE `nowIso` (415) / `db.transaction(...)` (420) — guarantees zero side effects; tests C1-C5 all pass | MATCH |
| 08.1-03: BOTH render paths emit hover-checkbox (Alpine bindings in direct, delegated listeners in virtual-scroller) | `gallery-view.js:39-50` (Alpine direct path) + `:110` (virtual-scroller string) + `:134-150` (delegated click + keydown listeners attached after `createVirtualScroller`) | MATCH |
| 08.1-03: existing `card-context-menu` listener unchanged | `context-menu.js:179` single document-level listener untouched | MATCH |
| All three SUMMARY commit hashes (`209d758`, `f3631bf`, `457b9d7`, `009c8f2`, `8b25795`, `6ad6f23`, `b9981b0`, `dbf0f95`, `a9f2b17`, `9024472`) verified in `git log` | 10 commits referenced; full-suite test results match orchestrator pre-check exactly (591 passed / 1 pre-existing failure). | MATCH |

All three SUMMARYs accurately reflect the codebase state. No drift detected.

### Test Suite Outcome

```
Test Files  1 failed | 65 passed | 2 skipped (68)
     Tests  1 failed | 591 passed | 10 todo (602)
```

The single failure: `tests/router.test.js > Screen content > vandalblast shows game tracker screen with setup` — pre-existing per `deferred-items.md`, predates Phase 08.1, root cause is collection-store field cleanup contamination from a prior test mounting the treasure-cruise screen. Out of scope for Phase 08.1 polish work.

**No new test failures introduced by Phase 08.1 work.** Matches orchestrator pre-check exactly.

## Gaps Summary

No gaps. All five ROADMAP success criteria are demonstrably TRUE at the code level — implementations are wired, data flows through real sources, tests assert the contracts, and the cross-plan @media coordination resolved cleanly. The plan-level scope guards (no `precon-decks.js`, no browse-by-set component) are also verified absent.

The verification status is **`human_needed`** rather than `passed` because the user explicitly walked Phase 8's UAT and committed to walking 08.1's UAT too. The five success criteria all involve user-perceived qualities (visual prominence, scroll feel, hover/focus motion, live-Scryfall walkthrough behaviour, end-to-end disabled-button no-op) that the test suite cannot fully replicate. The frontmatter `human_verification` block lists each criterion's UAT steps, expected behaviour, and rationale for human involvement.

---

_Verified: 2026-04-16T14:50:00Z_
_Verifier: Claude (gsd-verifier)_
