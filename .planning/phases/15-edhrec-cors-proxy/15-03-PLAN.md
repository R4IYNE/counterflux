---
phase: 15-edhrec-cors-proxy
plan: 15-03
type: execute
wave: 2
depends_on: [15-01, 15-02]
requirements: [PROXY-02, PROXY-04]
files_modified:
  - src/services/edhrec.js
  - src/services/spellbook.js
autonomous: true
must_haves:
  truths:
    - "The stale TODO comment at src/services/edhrec.js:4 ('In production, wire /api/edhrec to a serverless proxy or edge function') is gone — the proxy is now shipped, the comment is a lie"
    - "The stale TODO comment at src/services/spellbook.js:8 ('In production, wire /api/spellbook to a serverless proxy or edge function') is gone — same reason"
    - "Only the comment lines change; the EDHREC_BASE constant on src/services/edhrec.js:5 and the SPELLBOOK_BASE constant on src/services/spellbook.js:9 are byte-identical pre and post"
    - "All existing tests still pass (no regressions from the comment edit) — `npx vitest run tests/edhrec-service.test.js tests/spellbook-service.test.js` exits 0"
    - "`npm run build:check` exits 0 — the post-Phase-15 main bundle stays at ≤ 300 KB gz per scripts/assert-bundle-budget.js (PROXY-04 closure)"
    - "The `dist/` build output contains no `api/*` artifacts — Vercel's bundling correctly treats Functions as server-side-only and they do not enter the client bundle"
  artifacts:
    - path: "src/services/edhrec.js"
      provides: "EDHREC client service with stale 'wire to a serverless proxy' comment removed (line 3-4 originally)"
      contains: "const EDHREC_BASE = '/api/edhrec';"
    - path: "src/services/spellbook.js"
      provides: "Spellbook client service with stale 'wire to a serverless proxy' comment removed (line 7-8 originally)"
      contains: "const SPELLBOOK_BASE = '/api/spellbook';"
  key_links:
    - from: "src/services/edhrec.js EDHREC_BASE constant"
      to: "api/edhrec/[...path].js Function (Plan 15-01)"
      via: "URL alignment — both serve /api/edhrec/* in their respective environments (Vite dev / Vercel prod)"
      pattern: "EDHREC_BASE = '/api/edhrec'"
    - from: "src/services/spellbook.js SPELLBOOK_BASE constant"
      to: "api/spellbook/[...path].js Function (Plan 15-02)"
      via: "URL alignment — both serve /api/spellbook/* in their respective environments"
      pattern: "SPELLBOOK_BASE = '/api/spellbook'"
    - from: "tests/bundle-budget.test.js + scripts/assert-bundle-budget.js"
      to: "post-Phase-15 dist/assets/index-*.js"
      via: "npm run build:check enforces 300 KB gz main budget"
      pattern: "MAX_MAIN_BUNDLE\\|main:"
---

<objective>
Final cleanup wave for Phase 15. Two cosmetic edits to retire the now-stale TODO comments in the EDHREC + Spellbook client services (the Functions they reference are shipped in Plans 15-01 + 15-02), then a single end-to-end verification of `npm run build:check` to close PROXY-04 with the existing bundle-budget gate.

Purpose: 15-CONTEXT.md `<specifics>` line 145 explicitly calls out the stale comments as planner discretion; CONTEXT.md "Claude's Discretion" lists this cleanup. PROXY-04 reframe (D-06, D-07, D-08) defines the success criterion as "main bundle stays ≤ 300 KB gz post-change" via the existing `tests/bundle-budget.test.js` + `scripts/assert-bundle-budget.js` — no new test added. This plan executes that single verification.

Output: Two single-line edits to existing files + a recorded `npm run build:check` exit code 0 (the build pipeline already runs the bundle-budget assertion automatically per `package.json:9`).

Closes: PROXY-02 (final verification — both `EDHREC_BASE` and `SPELLBOOK_BASE` constants are byte-identical to their pre-Phase-15 values, only comments changed) and PROXY-04 (post-Phase-15 main bundle ≤ 300 KB gz). Combined with Plans 15-01 + 15-02, all five PROXY requirements are now closed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-edhrec-cors-proxy/15-CONTEXT.md
@src/services/edhrec.js
@src/services/spellbook.js
@scripts/assert-bundle-budget.js
@tests/bundle-budget.test.js
@package.json

<interfaces>
**Exact lines to delete from `src/services/edhrec.js`** (current state — line numbers as of the start of Phase 15):

Lines 3-4:
```js
// Proxy through Vite dev server to avoid CORS preflight rejection from CloudFront.
// In production, wire /api/edhrec to a serverless proxy or edge function.
```

The line that follows (line 5: `const EDHREC_BASE = '/api/edhrec';`) MUST remain unchanged. Only the two comment lines retire.

**Exact lines to delete from `src/services/spellbook.js`** (current state):

Lines 7-8:
```js
// Proxy through Vite dev server to avoid CORS issues.
// In production, wire /api/spellbook to a serverless proxy or edge function.
```

The line that follows (line 9: `const SPELLBOOK_BASE = '/api/spellbook';`) MUST remain unchanged.

**Optional — replacement comment** (recommended, not required): one short line above each constant explaining the dual-environment URL alignment. Example for `src/services/edhrec.js`:

```js
// EDHREC_BASE serves /api/edhrec/* in both environments:
// dev → Vite proxy (vite.config.js:7-12); prod → Vercel Function (api/edhrec/[...path].js).
```

And for `src/services/spellbook.js`:

```js
// SPELLBOOK_BASE serves /api/spellbook/* in both environments:
// dev → Vite proxy (vite.config.js:13-17); prod → Vercel Function (api/spellbook/[...path].js).
```

This documents the catch-all path alignment (D-01, D-02) so future readers know the production routing without spelunking.

**Bundle-budget gate** — package.json line 9:
```json
"build:check": "npm run build && node scripts/assert-bundle-budget.js"
```

The script in `scripts/assert-bundle-budget.js` enforces:
- `main: 300 * 1024` bytes gz (the constraint that PROXY-04 leans on)
- `vendor: 100 * 1024` bytes gz
- per-screen chunks etc.

Phase 15 success per D-20: this command continues to exit 0. The Vercel Functions in `api/*` do NOT enter `dist/assets/` (Vercel's bundling separates client and server), so the main bundle is byte-identical to pre-Phase-15 and the budget assertion is a sanity check, not a new pressure point.

**Existing tests that should still pass** — sanity check before declaring Phase 15 done:
- `tests/edhrec-service.test.js` — exercises the unchanged `EDHREC_BASE` URL via mocked fetch
- `tests/spellbook-service.test.js` — exercises the unchanged `SPELLBOOK_BASE` URL via mocked fetch
- `tests/api-edhrec-proxy.test.js` — Plan 15-01 Function tests
- `tests/api-spellbook-proxy.test.js` — Plan 15-02 Function tests
- `tests/bundle-budget.test.js` — meta-test on the budget script itself

The full `npm test` should also exit 0, but the targeted four files above are the load-bearing ones for this plan.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retire stale serverless-proxy TODO comments in src/services/edhrec.js and src/services/spellbook.js</name>
  <files>src/services/edhrec.js, src/services/spellbook.js</files>
  <read_first>
    - src/services/edhrec.js (full file — confirm current line 3-4 state matches the `<interfaces>` block)
    - src/services/spellbook.js (full file — confirm current line 7-8 state matches the `<interfaces>` block)
    - api/edhrec/[...path].js (Plan 15-01 — confirms the Function this comment now references)
    - api/spellbook/[...path].js (Plan 15-02 — same)
  </read_first>
  <action>
    Use the `Edit` tool for both files. Two edits total, each is a small comment swap.

    **Edit 1 — `src/services/edhrec.js`:**

    Replace these two consecutive lines (currently lines 3-4):
    ```
    // Proxy through Vite dev server to avoid CORS preflight rejection from CloudFront.
    // In production, wire /api/edhrec to a serverless proxy or edge function.
    ```

    With:
    ```
    // EDHREC_BASE serves /api/edhrec/* in both environments:
    // dev → Vite proxy (vite.config.js:7-12); prod → Vercel Function (api/edhrec/[...path].js).
    ```

    Net delta: 2 lines deleted, 2 lines added. Line 5 (the `EDHREC_BASE = '/api/edhrec';` constant) is byte-identical pre and post; line 1 (the `import` statement) and lines 6+ (rate-limiting + functions) are byte-identical too.

    **Edit 2 — `src/services/spellbook.js`:**

    Replace these two consecutive lines (currently lines 7-8):
    ```
    // Proxy through Vite dev server to avoid CORS issues.
    // In production, wire /api/spellbook to a serverless proxy or edge function.
    ```

    With:
    ```
    // SPELLBOOK_BASE serves /api/spellbook/* in both environments:
    // dev → Vite proxy (vite.config.js:13-17); prod → Vercel Function (api/spellbook/[...path].js).
    ```

    Net delta: 2 lines deleted, 2 lines added. Line 9 (the `SPELLBOOK_BASE = '/api/spellbook';` constant) is byte-identical pre and post; the JSDoc block at the top (lines 1-5), the `mapCombo` function (line 11+), and the `findDeckCombos` function are all byte-identical too.

    **Anti-patterns explicitly forbidden:**
    - DO NOT introduce a `import.meta.env.PROD` switch — D-02 forbids client-side environment branching, the catch-all path alignment is the entire point.
    - DO NOT change the `EDHREC_BASE` or `SPELLBOOK_BASE` constants in any way.
    - DO NOT touch any other line in either file.
    - DO NOT delete the TODO without a replacement comment — the dual-environment alignment is non-obvious to future readers and the new comments are the documentation that replaces the stale TODO.

    After both edits, run the targeted tests to confirm no regression:
    ```
    npx vitest run tests/edhrec-service.test.js tests/spellbook-service.test.js tests/api-edhrec-proxy.test.js tests/api-spellbook-proxy.test.js
    ```

    All four files should pass. If any fail, the comment edit broke something — STOP and inspect before committing.

    Commit with message `chore(15-03): retire stale serverless-proxy TODO comments (PROXY-02 closure)` via `gsd-tools commit`.
  </action>
  <acceptance_criteria>
    - `grep -c "wire /api/edhrec to a serverless proxy" src/services/edhrec.js` returns 0 (stale comment gone)
    - `grep -c "wire /api/spellbook to a serverless proxy" src/services/spellbook.js` returns 0 (stale comment gone)
    - `grep -c "Vercel Function" src/services/edhrec.js` returns at least 1 (replacement comment references the prod path)
    - `grep -c "Vercel Function" src/services/spellbook.js` returns at least 1 (same)
    - `grep -c "const EDHREC_BASE = '/api/edhrec';" src/services/edhrec.js` returns 1 (constant byte-identical)
    - `grep -c "const SPELLBOOK_BASE = '/api/spellbook';" src/services/spellbook.js` returns 1 (constant byte-identical)
    - `grep -c "import.meta.env" src/services/edhrec.js` returns 0 (no environment-branching introduced — D-02)
    - `grep -c "import.meta.env" src/services/spellbook.js` returns 0 (same)
    - The diff for each file is exactly 2 lines deleted + 2 lines added (use `git diff --stat src/services/edhrec.js src/services/spellbook.js` to sanity-check; expect ~4 insertions, ~4 deletions total)
    - `npx vitest run tests/edhrec-service.test.js tests/spellbook-service.test.js tests/api-edhrec-proxy.test.js tests/api-spellbook-proxy.test.js` exits 0
    - Git log shows a commit matching `chore(15-03): retire stale serverless-proxy TODO`
  </acceptance_criteria>
  <verify>
    <automated>grep -c "wire /api/edhrec to a serverless proxy" src/services/edhrec.js; grep -c "wire /api/spellbook to a serverless proxy" src/services/spellbook.js; grep -c "const EDHREC_BASE = '/api/edhrec';" src/services/edhrec.js; grep -c "const SPELLBOOK_BASE = '/api/spellbook';" src/services/spellbook.js; npx vitest run tests/edhrec-service.test.js tests/spellbook-service.test.js tests/api-edhrec-proxy.test.js tests/api-spellbook-proxy.test.js</automated>
  </verify>
  <done>Both stale "wire to a serverless proxy" comments are gone; both replacement comments reference the Vercel Function paths; both `*_BASE` constants are byte-identical pre and post; all four targeted vitest runs pass; commit landed.</done>
</task>

<task type="auto">
  <name>Task 2: Run npm run build:check to verify the post-Phase-15 main bundle stays ≤ 300 KB gz (PROXY-04 closure)</name>
  <files></files>
  <read_first>
    - scripts/assert-bundle-budget.js (full file — confirms BUDGETS.main = 300 * 1024 and the exit-1-on-violation behavior)
    - tests/bundle-budget.test.js (full file — meta-test that asserts the script exists with the right shape)
    - package.json (line 9 — `build:check` script chains `npm run build` + the assertion)
    - .planning/phases/15-edhrec-cors-proxy/15-CONTEXT.md decision D-06 / D-07 / D-08 (PROXY-04 reframe — "main bundle stays ≤ 300 KB gz post-change", no new test added)
  </read_first>
  <action>
    Run the existing build pipeline end-to-end to verify the bundle-budget gate continues to pass with the two new Vercel Functions on disk.

    **Step 1 — Confirm pre-existing tests pass:**

    ```
    npm test
    ```

    This runs `vitest run` across the entire test suite (per `package.json:11`). All tests should pass — no Phase 15 task should have introduced a regression. If anything fails, STOP and inspect; the Phase 15 changes should not affect any pre-existing test, and any new failure is a real bug that must be fixed before declaring the phase complete.

    **Step 2 — Run the bundle-budget gate:**

    ```
    npm run build:check
    ```

    This expands to `npm run build && node scripts/assert-bundle-budget.js` per `package.json:9`. Two-step pipeline:

    1. `vite build` produces `dist/` from `src/*` only — Vercel Functions in `api/*` are NOT included in this build (Vercel's deploy pipeline bundles them separately as Node.js Functions; the local `vite build` is the client-side build only).
    2. `node scripts/assert-bundle-budget.js` walks `dist/assets/` (gzip-sized) and asserts each file is under its budget. Main bundle budget = 300 KB gz per the BUDGETS table.

    Expected output: build success messages from Vite, then a budget summary from the assert script, then exit code 0.

    If `npm run build:check` exits non-zero:
    - If the failure is a Vite build error referencing files in `api/*`, that's a misconfiguration — Vercel Functions should NOT be picked up by `vite build`. STOP and check whether anything in `vite.config.js` was changed (it should not have been). Confirm `api/*` is not in any Vite include glob.
    - If the failure is a budget violation (`main: 305 KB > 300 KB`), STOP — Phase 15 should NOT have grown the main bundle. The Functions in `api/*` are server-side. If the main bundle grew, something inadvertently imported `api/*` from `src/*`, which the acceptance criteria of Plans 15-01 + 15-02 explicitly forbid. Investigate via `grep -rn "from ['\"]\\.\\./\\.\\./api" src/` (must return 0 matches).

    **Step 3 — Confirm `dist/` does NOT contain api/* artifacts:**

    ```
    ls dist/api 2>/dev/null && echo "ERROR: api/* leaked into client bundle" || echo "OK: dist/api does not exist"
    ```

    This is a sanity check — Vercel's deploy pipeline produces the Functions on the server side; the local `vite build` should produce a client-only `dist/`.

    **Step 4 — Sanity-check the Functions still exist on disk** (Plan 15-01 + Plan 15-02 outputs):

    ```
    test -f "api/edhrec/[...path].js" && test -f "api/spellbook/[...path].js" && echo "Both Functions present"
    ```

    **Step 5 — Commit a no-op marker if needed.** This task does not create any new files, but if `gsd-tools commit` requires a touched file, you can SKIP the commit step (Tasks 1's commit is sufficient evidence that Plan 15-03 ran). If your workflow tooling requires every task to commit, an empty commit with `git commit --allow-empty` is acceptable with message `chore(15-03): verify build:check + bundle budget post-Phase-15 (PROXY-04 closure)`.

    **Recording proof:** the orchestrator's SUMMARY.md should capture:
    - The exit code of `npm run build:check` (must be 0)
    - The reported main bundle size in KB gz (must be ≤ 300)
    - Confirmation that `dist/api` does not exist
    - Confirmation that all `npm test` tests passed
  </action>
  <acceptance_criteria>
    - `npm test` exits 0 (full suite passes — no regression from Phase 15)
    - `npm run build:check` exits 0 (build succeeds AND bundle-budget assertion passes)
    - `test -f "api/edhrec/[...path].js"` exits 0 (Plan 15-01 output present)
    - `test -f "api/spellbook/[...path].js"` exits 0 (Plan 15-02 output present)
    - `ls dist/api 2>/dev/null` returns nothing (Functions did NOT leak into the client bundle — Vercel server-side bundling boundary preserved)
    - The main bundle size reported by `scripts/assert-bundle-budget.js` is ≤ 300 KB gz (PROXY-04 numeric criterion)
    - `grep -rn "from ['\"]\\.\\./\\.\\./api" src/ 2>/dev/null | wc -l` returns 0 (no `src/*` file imports from `api/*`)
  </acceptance_criteria>
  <verify>
    <automated>npm test && npm run build:check && test -f "api/edhrec/[...path].js" && test -f "api/spellbook/[...path].js"</automated>
  </verify>
  <done>Full test suite passes; `npm run build:check` exits 0 with the main bundle reported ≤ 300 KB gz; `dist/api` does not exist; both Function files still on disk; PROXY-04 closed via the existing bundle-budget gate (no new test added per D-08).</done>
</task>

</tasks>

<verification>
Plan 15-03 is complete when ALL of these are true:

1. `grep -c "wire /api/edhrec to a serverless proxy" src/services/edhrec.js` returns 0
2. `grep -c "wire /api/spellbook to a serverless proxy" src/services/spellbook.js` returns 0
3. `grep -c "const EDHREC_BASE = '/api/edhrec';" src/services/edhrec.js` returns 1
4. `grep -c "const SPELLBOOK_BASE = '/api/spellbook';" src/services/spellbook.js` returns 1
5. `npm test` exits 0
6. `npm run build:check` exits 0
7. `ls dist/api 2>/dev/null` returns nothing
8. Git log contains 1 commit matching `chore(15-03): retire stale`

When all of Plans 15-01, 15-02, 15-03 are done, Phase 15 success criteria 2-5 from ROADMAP.md are observable on disk; success criterion 1 (live Production deploy verifies end-to-end) is Phase 16 territory (UAT-01..03 cover live verification).
</verification>

<success_criteria>
- Both stale TODO comments retired with informative replacement comments documenting the dual-environment URL alignment (PROXY-02 documentation closure).
- `EDHREC_BASE` and `SPELLBOOK_BASE` constants are byte-identical to their pre-Phase-15 state — Plans 15-01, 15-02, and 15-03 collectively required ZERO behavioral changes to the client services (PROXY-02 hard-gate).
- `npm run build:check` exits 0 — main bundle stays ≤ 300 KB gz post-Phase-15 (PROXY-04 closure via existing `tests/bundle-budget.test.js` + `scripts/assert-bundle-budget.js`; no new bundle test added per D-08).
- All five PROXY requirements (PROXY-01..05) are now closed across Plans 15-01, 15-02, 15-03 — Phase 15 ready for Phase 16's live-environment verification.
</success_criteria>

<output>
After completion, the orchestrator will create `.planning/phases/15-edhrec-cors-proxy/15-03-SUMMARY.md` summarising:
- The two comment-swap edits (line numbers, before/after)
- `npm test` exit code (0)
- `npm run build:check` exit code (0) + the recorded main bundle size in KB gz
- Confirmation that `git diff --stat src/services/edhrec.js src/services/spellbook.js` shows only ~2-line comment changes (no behavioral diff)
- Phase 15 closure status: Plans 15-01, 15-02, 15-03 all complete; PROXY-01..05 all checked; Phase 16 unblocked for live UAT verification
</output>
