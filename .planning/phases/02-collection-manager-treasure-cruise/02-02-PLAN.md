---
phase: 02-collection-manager-treasure-cruise
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/screens/treasure-cruise.js
  - src/components/stats-header.js
  - src/components/filter-bar.js
  - src/components/gallery-view.js
  - src/components/card-tile.js
  - src/components/table-view.js
  - src/components/set-completion.js
  - src/components/virtual-scroller.js
  - src/styles/utilities.css
  - src/main.js
  - index.html
  - tests/set-completion.test.js
autonomous: true
requirements: [COLL-02, COLL-03, COLL-04, COLL-11]

must_haves:
  truths:
    - "User sees stats header with total cards, unique cards, estimated value (EUR), and wishlist count"
    - "User can switch between Gallery, Table, and Sets views with toggle buttons"
    - "Gallery view shows card image tiles in a responsive grid with name, price, set, foil badge, qty badge"
    - "Table view shows sortable columns (Name, Set, Qty, Foil, Price EUR, Category) with click-to-sort"
    - "Set completion view shows per-set progress bars with owned/total counts, filterable by rarity"
    - "Virtual scrolling activates at 1,000+ items in gallery and table views"
    - "Filter bar allows sorting, colour filtering (WUBRG pips), and category filtering"
    - "Empty collection shows Mila empty state with onboarding message"
  artifacts:
    - path: "src/screens/treasure-cruise.js"
      provides: "Full collection manager screen layout"
      exports: ["mount"]
    - path: "src/components/gallery-view.js"
      provides: "Card image grid with virtual scrolling"
    - path: "src/components/table-view.js"
      provides: "Spreadsheet-style sortable table"
    - path: "src/components/set-completion.js"
      provides: "Per-set progress bars with rarity filter"
    - path: "src/components/virtual-scroller.js"
      provides: "Reusable virtual scrolling utility"
  key_links:
    - from: "src/screens/treasure-cruise.js"
      to: "src/stores/collection.js"
      via: "Alpine.store('collection') reactive bindings"
      pattern: "\\$store\\.collection"
    - from: "src/components/gallery-view.js"
      to: "src/components/virtual-scroller.js"
      via: "virtual scrolling for 1000+ cards"
    - from: "src/components/table-view.js"
      to: "src/components/virtual-scroller.js"
      via: "virtual scrolling for 1000+ rows"
---

<objective>
Build the main Treasure Cruise screen with three view modes (gallery, table, set completion), filter bar, stats header, view toggle, and virtual scrolling. This is the primary visual surface users interact with.

Purpose: Users need to see and browse their collection in multiple useful layouts. This plan delivers the read-only browsing experience.
Output: Fully functional collection screen with all three views, filtering, sorting, and virtual scrolling.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-collection-manager-treasure-cruise/02-CONTEXT.md
@.planning/phases/02-collection-manager-treasure-cruise/02-UI-SPEC.md
@.planning/phases/02-collection-manager-treasure-cruise/02-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From src/stores/collection.js (created in Plan 01):
```javascript
export function initCollectionStore();
// Alpine.store('collection') provides:
// .entries, .viewMode, .sortBy, .filters, .analyticsOpen, .loading
// .filtered (getter), .sorted (getter), .stats (getter)
// .loadEntries(), .addCard(), .editEntry(), .deleteEntry(), .addBatch()
// .setViewMode(mode), .setSortBy(sort), .toggleColour(colour), .setCategory(category)
```

From src/db/card-accessor.js:
```javascript
export function getCardImage(card, face = 0, size = 'normal');
export function getCardName(card);
export function getCardThumbnail(card);
export function getCardManaCost(card);
```

From src/components/empty-state.js:
```javascript
export function renderEmptyState(container, { heading, body });
```

From src/utils/mana.js:
```javascript
// window.renderManaCost(manaCostString) -> HTML string with mana-font icons
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Virtual scroller utility + screen layout + stats header + filter bar + view toggle</name>
  <files>src/components/virtual-scroller.js, src/screens/treasure-cruise.js, src/components/stats-header.js, src/components/filter-bar.js, src/styles/utilities.css, src/main.js, index.html</files>
  <read_first>
    - src/screens/treasure-cruise.js (current placeholder to replace)
    - src/components/empty-state.js (empty state component to reuse)
    - src/stores/app.js (Alpine store pattern, toast, screen registration)
    - src/main.js (current imports and Alpine init)
    - index.html (current HTML structure -- sidebar, topbar, main content area)
    - src/styles/utilities.css (existing custom CSS utilities to extend)
    - .planning/phases/02-collection-manager-treasure-cruise/02-UI-SPEC.md (full design contract: component inventory, interaction patterns, copywriting, CSS utilities)
    - .planning/phases/02-collection-manager-treasure-cruise/02-CONTEXT.md (D-06 simplified layout, D-10 virtual scroller reuse, D-11 toggle discretion, D-17 inline analytics)
  </read_first>
  <action>
    1. Create `src/components/virtual-scroller.js`:
    Extract a reusable virtual scroller utility. It should export a function `createVirtualScroller(container, { itemHeight, renderItem, getItemCount, overscan = 5 })` that:
    - Creates a scroll container div with `overflow-y: auto; height: 100%`
    - Creates an inner div sized to `itemHeight * getItemCount()` for scroll height
    - On scroll, calculates visible range (startIndex, endIndex) based on scrollTop and container height
    - Renders only visible items + overscan buffer by calling renderItem(index) for each
    - Returns { update() } to re-render when data changes, { destroy() } for cleanup
    - For grid layouts (gallery), accept optional `columns` parameter and calculate itemHeight as row height, rendering `columns` items per row.

    2. Replace `src/screens/treasure-cruise.js`:
    The mount(container) function should:
    - Call `Alpine.store('collection').loadEntries()` on mount
    - Set `Alpine.store('app').currentScreen = 'treasure-cruise'`
    - Render the screen layout HTML with Alpine bindings:
      - Screen header: Syne 20px 700 `Archive Manifest`, label 700 `MASTER REPOSITORY SUMMARY`
      - Stats header component area (x-data referencing stats)
      - View toggle buttons (GALLERY / TABLE / SETS) -- use segmented toggle buttons per D-11, styled as JetBrains Mono 11px 700 uppercase. Active: `bg-[rgba(13,82,189,0.1)] text-[#0D52BD] border-b-2 border-[#0D52BD]`. Inactive: `text-[#7A8498] hover:text-[#EAECEE]`.
      - Filter bar area
      - Main content area that switches view based on `$store.collection.viewMode`
      - Empty state when `$store.collection.entries.length === 0`
    - Wire initCollectionStore() in src/main.js (import and call before Alpine.start())

    3. Create `src/components/stats-header.js`:
    Export a function `renderStatsHeader()` that returns an HTML string with Alpine reactive bindings:
    - Container: surface-hover bg (#1C1F28), left border 3px primary (#0D52BD), padding 24px
    - Four stat boxes in a horizontal flex row with gap-32:
      - TOTAL CARDS: label 700 `TOTAL CARDS`, display value using `$store.collection.stats.totalCards` formatted with thousands separator, Syne 48px 700 primary colour
      - UNIQUE CARDS: label 700, value from `$store.collection.stats.uniqueCards`
      - ESTIMATED VALUE: label 700, value `EUR {amount}` from `$store.collection.stats.estimatedValue` formatted with 2 decimal places and thousands separator
      - WISHLIST: label 700, value from `$store.collection.stats.wishlistCount`

    4. Create `src/components/filter-bar.js`:
    Export a function `renderFilterBar()` returning HTML string with Alpine bindings:
    - Container: surface-hover bg, padding 16px, flex row with items-center and gap between groups
    - Sort dropdown: label 700 `SORT:` followed by a `<select>` bound to `$store.collection.sortBy`. Options: `PRICE DESCENDING` (price-desc), `PRICE ASCENDING` (price-asc), `NAME A-Z` (name-asc), `NAME Z-A` (name-desc), `SET RELEASE` (set-asc), `DATE ADDED` (date-desc). Style select with surface bg, ghost border, text-primary, JetBrains Mono 11px.
    - Colour filter pips: 5 square buttons (24x24px) for W/U/B/R/G. Each uses its MTG colour hex as background when active, surface-hover bg when inactive. Active state: filled with MTG colour + 2px ring of same colour. Toggle on click via `$store.collection.toggleColour('W')` etc. Each MUST have `aria-label="Filter by {colour name}"` and `title="{colour name}"` per UI-SPEC.
    - Category filter: label 700 `CATEGORY:` followed by three buttons: ALL / OWNED / WISHLIST. Active: primary text. Inactive: text-muted. Bound to `$store.collection.setCategory()`.
    - Action buttons (right-aligned): ADD CARD, MASS ENTRY, IMPORT CSV, EXPORT CSV. Primary bg for ADD CARD, surface-hover bg for others. All JetBrains Mono 11px 700 uppercase. Click handlers: `$store.collection.addCardOpen = true`, `$store.collection.massEntryOpen = true`, `$store.collection.importOpen = true`, and `$dispatch('export-csv')` respectively.

    5. Add Phase 2 CSS utilities to `src/styles/utilities.css`:
    ```css
    .card-tile-hover {
      transition: all 500ms;
    }
    .card-tile-hover:hover {
      border-color: rgba(13, 82, 189, 0.5);
    }
    .card-tile-hover:hover img {
      transform: scale(1.1);
      opacity: 1;
    }
    .progress-glow {
      box-shadow: 0 0 15px rgba(13, 82, 189, 0.5);
    }
    .foil-badge {
      background: rgba(226, 56, 56, 0.2);
      color: #E23838;
      font-family: 'JetBrains Mono Variable', monospace;
      font-size: 11px;
      text-transform: uppercase;
      letter-spacing: 0.15em;
      padding: 4px 8px;
    }
    .qty-badge {
      background: rgba(13, 82, 189, 0.8);
      color: #EAECEE;
      font-family: 'JetBrains Mono Variable', monospace;
      font-size: 11px;
      font-weight: 700;
      padding: 4px 8px;
      position: absolute;
      top: 8px;
      right: 8px;
    }
    .terminal-input {
      font-family: 'JetBrains Mono Variable', monospace;
      font-size: 14px;
      background: #0B0C10;
      border: 1px solid #2A2D3A;
      color: #EAECEE;
      padding: 16px;
      resize: vertical;
    }
    ```

    6. Update `src/main.js`: Import and call `initCollectionStore()` from `src/stores/collection.js` before `Alpine.start()`.

    7. Update `index.html` if needed: Ensure main content area renders the treasure-cruise screen when navigated to. The screen's mount() function handles all internal rendering, so the main content `<div id="main-content">` should already be wired via the Navigo router.
  </action>
  <verify>
    <automated>cd "D:/Vibe Coding/counterflux" && rtk vitest run tests/virtual-scroller.test.js --reporter=verbose && rtk npm run build</automated>
  </verify>
  <acceptance_criteria>
    - src/components/virtual-scroller.js contains `export function createVirtualScroller(`
    - src/screens/treasure-cruise.js contains `Archive Manifest` and `MASTER REPOSITORY SUMMARY`
    - src/screens/treasure-cruise.js contains `$store.collection.viewMode`
    - src/components/stats-header.js contains `TOTAL CARDS` and `UNIQUE CARDS` and `ESTIMATED VALUE` and `WISHLIST`
    - src/components/filter-bar.js contains `aria-label="Filter by White"` and `aria-label="Filter by Blue"`
    - src/components/filter-bar.js contains `SORT:` and `CATEGORY:` and `ADD CARD` and `MASS ENTRY` and `IMPORT CSV` and `EXPORT CSV`
    - src/styles/utilities.css contains `.card-tile-hover` and `.progress-glow` and `.foil-badge` and `.qty-badge` and `.terminal-input`
    - src/main.js contains `initCollectionStore`
    - tests/virtual-scroller.test.js passes with window calculation logic tests
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Screen layout renders with stats header, view toggle, and filter bar. Virtual scroller utility ready for use with passing tests for window calculation logic. CSS utilities added. Collection store initialised on app start. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Gallery view, table view, set completion view with virtual scrolling</name>
  <files>src/components/gallery-view.js, src/components/card-tile.js, src/components/table-view.js, src/components/set-completion.js, tests/set-completion.test.js</files>
  <read_first>
    - src/screens/treasure-cruise.js (just built -- understand mount structure and where views plug in)
    - src/components/virtual-scroller.js (just built -- API to use)
    - src/stores/collection.js (store API: sorted getter, viewMode, filters)
    - src/db/card-accessor.js (getCardImage, getCardThumbnail, getCardName for rendering)
    - .planning/phases/02-collection-manager-treasure-cruise/02-UI-SPEC.md (gallery grid spec, table spec, set completion spec, card-tile spec, interaction patterns)
    - .planning/phases/02-collection-manager-treasure-cruise/02-CONTEXT.md (D-07 gallery tiles, D-08 table columns, D-09 set completion, D-10 virtual scroller reuse)
    - tests/set-completion.test.js (scaffold to fill in)
  </read_first>
  <action>
    1. Create `src/components/card-tile.js`:
    Export `renderCardTile(entry)` returning HTML string for a single gallery card tile:
    - Container: relative, surface bg (#14161C), ghost border (#2A2D3A), `card-tile-hover` class, cursor-pointer
    - Image area: `<img>` with src from `getCardThumbnail(entry.card)` or `getCardImage(entry.card, 0, 'normal')`, aspect-[63/88], object-cover, opacity-80 default (hover to 100 via CSS), overflow-hidden for scale effect
    - Gradient overlay at bottom of image: `bg-gradient-to-t from-[#14161C] to-transparent`
    - If quantity > 1: qty-badge class overlay `x{qty}` top-right
    - If foil: foil-badge `FOIL` positioned bottom-left of image area
    - Below image: padding 8px
      - Name: Space Grotesk 14px 700, text-primary (#EAECEE), truncate with ellipsis (single line)
      - Price: JetBrains Mono 11px 400, primary colour (#0D52BD), `EUR {price}`. Use entry.foil ? entry.card?.prices?.eur_foil : entry.card?.prices?.eur. Show `--` if no price.
      - Set: JetBrains Mono 11px 400, text-dim (#4A5064), set name uppercase
    - Click: `$store.search.selectResult(entry.card)` to open flyout
    - Right-click: dispatch custom event for context menu (implemented in Plan 03)

    2. Create `src/components/gallery-view.js`:
    Export `renderGalleryView()` returning HTML string or a mount function:
    - Responsive grid: `grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 xl:grid-cols-5 2xl:grid-cols-6` with gap-[24px]
    - Iterate over `$store.collection.sorted` with Alpine x-for
    - Each item renders a card-tile
    - For virtual scrolling: if sorted length > 200 (lower threshold for smooth experience), use the virtual scroller from virtual-scroller.js. Otherwise render all items directly.
    - The virtual scroller should be integrated by wrapping the grid container and only rendering visible rows of cards.

    3. Create `src/components/table-view.js`:
    Export `renderTableView()` returning HTML string:
    - Table with ghost-border row separators (border-bottom: 1px solid #2A2D3A)
    - Header row: JetBrains Mono 11px 700 uppercase, text-muted. Columns per D-08:
      - NAME (sortable, click calls `$store.collection.setSortBy('name-asc')` or toggles to 'name-desc')
      - SET (sortable)
      - QTY (sortable)
      - FOIL (not sortable, just displays yes/no or foil badge)
      - PRICE (EUR) (sortable)
      - CATEGORY (sortable)
    - Active sort column header: primary colour text. Sort direction icon: Material Symbol `arrow_upward` or `arrow_downward` (11px).
    - Data rows: iterate `$store.collection.sorted` with x-for. Each row:
      - Name: Space Grotesk 14px 400 text-primary
      - Set: JetBrains Mono 11px 400 text-muted, set name
      - Qty: JetBrains Mono 11px 400
      - Foil: Show foil-badge `FOIL` or empty
      - Price: JetBrains Mono 11px 400 primary, `EUR {price}`
      - Category: JetBrains Mono 11px 700 uppercase, `OWNED` or `WISHLIST`
    - Row hover: surface-hover bg
    - Row click: open card detail flyout
    - Row right-click: context menu event
    - Virtual scrolling for 1,000+ rows using virtual-scroller.js (fixed row height ~40px)

    4. Create `src/components/set-completion.js`:
    Export `renderSetCompletionView()` returning HTML:
    - Rarity filter buttons at top: ALL / COMMON / UNCOMMON / RARE / MYTHIC. JetBrains Mono 11px 700 uppercase. Active: primary bg. Default: ALL.
    - List of sets sorted by most-owned. Each row:
      - Set icon via keyrune: `<i class="ss ss-{setCode}"></i>`
      - Set name: Space Grotesk 14px 700
      - Progress bar: full-width, 4px height, bg border-ghost, fill primary with progress-glow class. Width = (owned/total)*100%
      - Labels: `{owned}/{total}` (JetBrains Mono 11px 400), `{pct}% COMPLETE` (JetBrains Mono 11px 700 primary)
    - Click on set row: filters collection to that set and switches to gallery view
    - Computing set completion requires:
      - Query `db.cards` grouped by set to get total cards per set (and per rarity within set)
      - Query `db.collection` entries grouped by set (via joined card data) to get owned counts
      - Export computation logic as `computeSetCompletion(entries, allCards, rarityFilter)` for testability

    5. Fill in `tests/set-completion.test.js`:
    - Test: computeSetCompletion with 3 owned out of 10 total in set 'mh2' returns { set: 'mh2', owned: 3, total: 10, pct: 30 }
    - Test: filtering by 'mythic' rarity returns only mythic cards in denominator
    - Test: sets with 0 owned are included but with pct 0
    - Use fake-indexeddb with seeded card data
  </action>
  <verify>
    <automated>cd "D:/Vibe Coding/counterflux" && rtk vitest run tests/set-completion.test.js --reporter=verbose && rtk npm run build</automated>
  </verify>
  <acceptance_criteria>
    - src/components/gallery-view.js contains `grid-cols-2` and `md:grid-cols-3` and `lg:grid-cols-4`
    - src/components/card-tile.js contains `card-tile-hover` and `foil-badge` and `qty-badge`
    - src/components/card-tile.js contains `EUR` for price display
    - src/components/table-view.js contains `NAME` and `SET` and `QTY` and `FOIL` and `PRICE (EUR)` and `CATEGORY`
    - src/components/table-view.js contains `arrow_upward` or `arrow_downward` for sort indicators
    - src/components/set-completion.js contains `% COMPLETE` and `progress-glow`
    - src/components/set-completion.js contains `computeSetCompletion` exported function
    - tests/set-completion.test.js has at least 3 test cases and exits 0
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>All three view modes render correctly with data from collection store. Gallery shows responsive card grid with hover effects. Table shows sortable columns with sort indicators. Set completion shows progress bars with rarity filtering. Virtual scrolling activated for large collections. Set completion tests pass.</done>
</task>

</tasks>

<verification>
- `npx vitest run` passes (all tests green)
- `npm run build` exits 0
- Collection screen renders with stats header, filter bar, and all three view modes
- Virtual scroller utility handles both grid and list layouts
</verification>

<success_criteria>
Users can navigate to Treasure Cruise and see the full collection browsing interface: stats summary, view toggle (Gallery/Table/Sets), filter bar with sort/colour/category controls, and data displayed in the selected view mode. Empty state shows Mila when collection is empty.
</success_criteria>

<output>
After completion, create `.planning/phases/02-collection-manager-treasure-cruise/02-02-SUMMARY.md`
</output>
