---
phase: 04-intelligence-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema.js
  - src/services/edhrec.js
  - tests/fixtures/edhrec-prossh.json
  - tests/edhrec-service.test.js
  - tests/salt-score.test.js
autonomous: true
requirements: [INTEL-01, INTEL-05]
must_haves:
  truths:
    - "EDHREC synergy data can be fetched and cached for a commander"
    - "Salt score is available from EDHREC commander page"
    - "Rate limiting prevents excessive EDHREC requests"
    - "Cached data is returned on subsequent requests within 7 days"
    - "Graceful degradation returns null on EDHREC failure"
  artifacts:
    - path: "src/db/schema.js"
      provides: "Dexie schema v4 with edhrec_cache, combo_cache, card_salt_cache tables"
      contains: "db.version(4)"
    - path: "src/services/edhrec.js"
      provides: "EDHREC API client with caching and rate limiting"
      exports: ["getCommanderSynergies", "getCardSalt"]
    - path: "tests/fixtures/edhrec-prossh.json"
      provides: "Sample EDHREC commander JSON response fixture"
      contains: "highsynergycards"
    - path: "tests/edhrec-service.test.js"
      provides: "Unit tests for EDHREC synergy fetch, cache, parse"
    - path: "tests/salt-score.test.js"
      provides: "Unit tests for salt score aggregation and normalization"
  key_links:
    - from: "src/services/edhrec.js"
      to: "src/db/schema.js"
      via: "Dexie edhrec_cache table"
      pattern: "db\\.edhrec_cache"
---

<objective>
Build the EDHREC service layer with Dexie caching, rate limiting, and test coverage.

Purpose: Provides synergy suggestions (INTEL-01) and salt score data (INTEL-05) from EDHREC's JSON endpoints, cached in IndexedDB for offline access. This is one of two parallel service plans that feed the intelligence store in Plan 03.

Output: `src/services/edhrec.js`, Dexie schema v4, test fixtures, passing unit tests
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-intelligence-layer/04-CONTEXT.md
@.planning/phases/04-intelligence-layer/04-RESEARCH.md
@.planning/phases/04-intelligence-layer/04-VALIDATION.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/db/schema.js (current v3):
```javascript
import Dexie from 'dexie';
export const db = new Dexie('counterflux');
db.version(3).stores({
  cards: 'id, name, oracle_id, set, collector_number, cmc, color_identity, type_line, [set+collector_number]',
  meta: 'key',
  collection: '++id, scryfall_id, category, foil, [scryfall_id+foil], [scryfall_id+category]',
  decks: '++id, name, format, updated_at',
  deck_cards: '++id, deck_id, scryfall_id, [deck_id+scryfall_id]'
});
```

From src/services/currency.js:
```javascript
export function eurToGbp(eurAmount) { /* ... */ }
export function eurToGbpValue(eurFloat) { /* ... */ }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Dexie schema v4 + EDHREC service with rate-limited caching</name>
  <files>src/db/schema.js, src/services/edhrec.js, tests/fixtures/edhrec-prossh.json, tests/edhrec-service.test.js</files>
  <read_first>
    - src/db/schema.js (current v3 schema — must copy ALL existing stores into v4)
    - src/services/currency.js (eurToGbpValue for price conversion pattern)
    - .planning/phases/04-intelligence-layer/04-RESEARCH.md (EDHREC JSON response shape at line 406-443, code examples at line 108-174)
    - .planning/phases/04-intelligence-layer/04-CONTEXT.md (D-01, D-02, D-03, D-04 locked decisions)
  </read_first>
  <behavior>
    - Test: getCommanderSynergies('prossh-skyraider-of-kher') returns synergies array with name, synergy (decimal), inclusion (number) for each entry
    - Test: getCommanderSynergies returns cached data on second call without fetching
    - Test: getCommanderSynergies returns null/empty on network failure (graceful degradation per D-03)
    - Test: rate limiter enforces minimum 200ms between requests
    - Test: cache expires after 7 days (CACHE_TTL_MS = 604800000)
    - Test: commander name sanitization: "Prossh, Skyraider of Kher" -> "prossh-skyraider-of-kher"
    - Test: commander name sanitization: "Zur the Enchanter" -> "zur-the-enchanter"
  </behavior>
  <action>
    1. **Dexie schema v4** in `src/db/schema.js`: Add `db.version(4).stores({...})` that copies ALL v3 tables verbatim and adds three new tables:
       - `edhrec_cache: 'commander'` (primary key is sanitized commander name)
       - `combo_cache: 'deck_id'` (primary key is deck ID)
       - `card_salt_cache: 'sanitized'` (primary key is sanitized card name)
    
    2. **Test fixture** `tests/fixtures/edhrec-prossh.json`: Create a trimmed EDHREC JSON response matching the verified shape from RESEARCH.md lines 406-443. Include:
       - `container.json_dict.card.salt` = 0.8
       - `container.json_dict.card.name` = "Prossh, Skyraider of Kher"
       - `container.json_dict.card.color_identity` = ["B","R","G"]
       - `container.json_dict.cardlists` array with at least `highsynergycards` tag containing 3 sample cardviews:
         - { name: "Impact Tremors", synergy: 0.63, inclusion: 4477, num_decks: 4477, url: "/cards/impact-tremors", sanitized: "impact-tremors" }
         - { name: "Purphoros, God of the Forge", synergy: 0.58, inclusion: 3200, num_decks: 3200, url: "/cards/purphoros-god-of-the-forge", sanitized: "purphoros-god-of-the-forge" }
         - { name: "Zulaport Cutthroat", synergy: 0.45, inclusion: 2800, num_decks: 2800, url: "/cards/zulaport-cutthroat", sanitized: "zulaport-cutthroat" }
    
    3. **EDHREC service** `src/services/edhrec.js`: Create module with:
       - `EDHREC_BASE = 'https://json.edhrec.com'`
       - `CACHE_TTL_MS = 7 * 24 * 60 * 60 * 1000` (7 days)
       - `REQUEST_DELAY_MS = 200` (rate limit)
       - `sanitizeCommanderName(name)`: `name.toLowerCase().replace(/[',]/g, '').replace(/[^a-z0-9]+/g, '-').replace(/-+$/, '').replace(/^-+/, '')`
       - `rateLimitedFetch(url)`: Enforces minimum REQUEST_DELAY_MS between fetch calls. On failure, returns null (per D-03).
       - `export async function getCommanderSynergies(commanderName)`:
         - Sanitize name, check `db.edhrec_cache.get(sanitized)`, return cached if within TTL
         - Fetch `${EDHREC_BASE}/pages/commanders/${sanitized}.json`
         - Parse: extract `highsynergycards` from `container.json_dict.cardlists`, map each cardview to `{ name, synergy, inclusion, num_decks, url, sanitized }`
         - Extract commander salt from `container.json_dict.card.salt`
         - Store in `db.edhrec_cache.put({ commander: sanitized, data: { synergies, commanderSalt, colorIdentity }, fetched_at: Date.now() })`
         - On any error: return `{ synergies: [], commanderSalt: null, colorIdentity: [], error: true }`
       - `export async function getCardSalt(cardName)`:
         - Sanitize name, check `db.card_salt_cache.get(sanitized)`
         - Fetch `${EDHREC_BASE}/cards/${sanitized}`
         - Extract `salt` field, cache in `db.card_salt_cache.put({ sanitized, salt, fetched_at: Date.now() })`
         - On failure: return null
       - `export function normalizeSalt(rawSalt)`: `Math.min(10, Math.round(rawSalt * 2.5))` — converts raw EDHREC decimal to 0-10 display scale
    
    4. **Tests** `tests/edhrec-service.test.js`: Mock global fetch using `vi.fn()`. Import edhrec functions. Test all behaviors listed above. Use the fixture JSON for mock responses. Test sanitization as pure function.

    Per D-01: Use EDHREC JSON endpoints as primary data source.
    Per D-02: Cache in IndexedDB keyed by commander name.
    Per D-03: Return safe fallback on failure — `{ synergies: [], commanderSalt: null, error: true }`.
    Per D-04: 200ms minimum between requests.
  </action>
  <verify>
    <automated>npx vitest run tests/edhrec-service.test.js</automated>
  </verify>
  <acceptance_criteria>
    - src/db/schema.js contains `db.version(4).stores({` with all v3 tables plus `edhrec_cache: 'commander'`, `combo_cache: 'deck_id'`, `card_salt_cache: 'sanitized'`
    - src/services/edhrec.js exports `getCommanderSynergies`, `getCardSalt`, `normalizeSalt`, `sanitizeCommanderName`
    - src/services/edhrec.js contains `CACHE_TTL_MS = 7 * 24 * 60 * 60 * 1000`
    - src/services/edhrec.js contains `REQUEST_DELAY_MS = 200`
    - src/services/edhrec.js contains `https://json.edhrec.com`
    - tests/fixtures/edhrec-prossh.json contains `highsynergycards`
    - tests/edhrec-service.test.js exits 0
  </acceptance_criteria>
  <done>EDHREC service fetches synergy data and salt scores with 7-day caching, 200ms rate limiting, and graceful degradation. All tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Salt score aggregation and normalization tests</name>
  <files>tests/salt-score.test.js</files>
  <read_first>
    - src/services/edhrec.js (just created — normalizeSalt function)
    - .planning/phases/04-intelligence-layer/04-RESEARCH.md (salt normalization at line 509-511, anti-pattern at line 282-284)
    - .planning/phases/04-intelligence-layer/04-UI-SPEC.md (salt display states at line 130-138)
  </read_first>
  <behavior>
    - Test: normalizeSalt(0.8) returns 2 (mild range)
    - Test: normalizeSalt(1.5) returns 4 (spicy range)
    - Test: normalizeSalt(3.2) returns 8 (critical range)
    - Test: normalizeSalt(5.0) returns 10 (capped at 10)
    - Test: normalizeSalt(0) returns 0
    - Test: normalizeSalt(null) returns 0 or handles gracefully
    - Test: aggregateDeckSalt([0.8, 1.5, 0.2, 3.0]) returns average then normalized
    - Test: aggregateDeckSalt([]) returns null
  </behavior>
  <action>
    Create `tests/salt-score.test.js` that imports `normalizeSalt` from `src/services/edhrec.js`. Also create and export an `aggregateDeckSalt(saltValues)` function in `src/services/edhrec.js` that:
    - Filters out null/undefined values
    - Returns null if no valid values
    - Computes arithmetic mean of remaining values
    - Returns `normalizeSalt(mean)`

    Add the `aggregateDeckSalt` export to edhrec.js alongside existing functions.

    Test cases:
    - normalizeSalt: 0 -> 0, 0.8 -> 2, 1.5 -> 4, 2.0 -> 5, 3.2 -> 8, 4.0 -> 10, 5.0 -> 10
    - aggregateDeckSalt: empty array -> null, [null, undefined] -> null, [0.8, 1.5, 0.2, 3.0] -> normalizeSalt(1.375) = 3, mixed with nulls filters correctly
  </action>
  <verify>
    <automated>npx vitest run tests/salt-score.test.js</automated>
  </verify>
  <acceptance_criteria>
    - src/services/edhrec.js exports `aggregateDeckSalt`
    - tests/salt-score.test.js contains tests for normalizeSalt edge cases (0, null, cap at 10)
    - tests/salt-score.test.js contains tests for aggregateDeckSalt (empty, mixed nulls, valid array)
    - tests/salt-score.test.js exits 0
  </acceptance_criteria>
  <done>Salt score normalization and aggregation fully tested with edge cases. normalizeSalt converts raw EDHREC decimal to 0-10 scale, aggregateDeckSalt averages then normalizes.</done>
</task>

</tasks>

<verification>
All tests pass: `npx vitest run tests/edhrec-service.test.js tests/salt-score.test.js`
Schema v4 includes all v3 tables plus new cache tables.
EDHREC service handles happy path, cache hit, and network failure.
</verification>

<success_criteria>
- EDHREC synergy fetch returns structured data with lift scores (INTEL-01)
- Salt score normalization converts 0-4 raw range to 0-10 display scale (INTEL-05)
- 7-day cache prevents redundant API calls (D-02)
- Graceful degradation on EDHREC failure returns safe fallback (D-03)
- Rate limiting enforces 200ms minimum between requests (D-04)
- All tests green
</success_criteria>

<output>
After completion, create `.planning/phases/04-intelligence-layer/04-01-SUMMARY.md`
</output>
