---
phase: 03-deck-builder-thousand-year-storm
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - src/components/deck-editor.js
  - src/components/deck-search-panel.js
  - src/components/deck-centre-panel.js
  - src/components/deck-card-tile.js
  - src/components/deck-context-menu.js
  - src/components/tag-manager.js
  - src/screens/thousand-year.js
autonomous: true
requirements:
  - DECK-01
  - DECK-02
  - DECK-03
  - DECK-04
  - DECK-06
  - DECK-07
  - DECK-18
  - DECK-19

must_haves:
  truths:
    - "Three-panel layout fills content area: search 280px left, centre flexible, analytics 280px right"
    - "Search panel filters by colour identity and returns results with autocomplete"
    - "In Collection Only toggle filters results to owned cards"
    - "Centre panel shows cards grouped by type with headers and card counts"
    - "User can toggle between visual grid and compact list views"
    - "Drag-and-drop moves cards between type groups via SortableJS"
    - "Right-click context menu on cards shows deck-specific actions"
    - "Card count tracker shows current/total with slots remaining"
  artifacts:
    - path: "src/components/deck-editor.js"
      provides: "Three-panel layout orchestrator"
      exports: ["renderDeckEditor"]
    - path: "src/components/deck-search-panel.js"
      provides: "Search panel with filters and In Collection toggle"
      exports: ["renderDeckSearchPanel"]
    - path: "src/components/deck-centre-panel.js"
      provides: "The 99 with grid/list views and SortableJS"
      exports: ["renderDeckCentrePanel"]
    - path: "src/components/deck-card-tile.js"
      provides: "Card tile with owned/missing dots"
      exports: ["renderDeckCardTile"]
  key_links:
    - from: "src/components/deck-editor.js"
      to: "src/stores/deck.js"
      via: "Alpine.store('deck').activeDeck"
      pattern: "Alpine\\.store\\('deck'\\)"
    - from: "src/components/deck-search-panel.js"
      to: "src/db/search.js"
      via: "searchCards() with colour identity filter"
      pattern: "searchCards"
    - from: "src/components/deck-centre-panel.js"
      to: "sortablejs"
      via: "new Sortable() on group containers"
      pattern: "new Sortable"
    - from: "src/components/deck-search-panel.js"
      to: "src/stores/collection.js"
      via: "collection entries for In Collection filter"
      pattern: "Alpine\\.store\\('collection'\\)"
---

<objective>
Build the three-panel deck editor: search panel with colour identity filtering and In Collection toggle, centre panel with type-grouped cards in grid/list views with SortableJS drag-and-drop, card tiles with owned/missing indicators, context menu, and tag management. This is the core editing experience.

Purpose: The three-panel editor is where users spend most of their time building decks. It implements the primary interaction loop: search for cards, add to deck, organise by type, and see the deck take shape.
Output: Functional three-panel editor where users can search, add, organise, and manage cards in their deck.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-deck-builder-thousand-year-storm/03-CONTEXT.md
@.planning/phases/03-deck-builder-thousand-year-storm/03-UI-SPEC.md
@.planning/phases/03-deck-builder-thousand-year-storm/03-01-SUMMARY.md
@.planning/phases/03-deck-builder-thousand-year-storm/03-02-SUMMARY.md

<interfaces>
From src/stores/deck.js (Plan 01):
```javascript
// Alpine.store('deck'):
//   activeDeck: { id, name, format, deck_size, commander_id, partner_id, companion_id, color_identity, tags }
//   activeCards: [{ id, deck_id, scryfall_id, quantity, tags, sort_order, card: {...}, owned: bool }]
//   viewMode: 'grid' | 'list'
//   get cardCount()
//   get slotsRemaining()
//   get groupedByType() -- returns { Creature: [...], Instant: [...], ... } sorted by TYPE_ORDER
//   get analytics()
//   addCard(scryfallId, tags), removeCard(deckCardId), updateCardTags(deckCardId, tags)
//   reorderCard(deckCardId, newSortOrder)
```

From src/db/search.js:
```javascript
export async function searchCards(query, options);
// Returns array of Scryfall card objects from IndexedDB
```

From src/components/card-tile.js:
```javascript
export function renderCardTile(card, options);
// Renders card image, name, price, foil badge
```

From src/components/context-menu.js:
```javascript
export function initContextMenu(container);
// Pattern: custom DOM event -> render menu at cursor -> execute action
```

From src/utils/type-classifier.js (Plan 01):
```javascript
export function classifyType(typeLine);
export const TYPE_ORDER;
```

From src/utils/tag-heuristics.js (Plan 01):
```javascript
export function suggestTags(oracleText);
export const DEFAULT_TAGS;
```

From src/db/card-accessor.js:
```javascript
// getCardName(card), getCardImage(card), getCardManaCost(card), etc.
// Handles all Scryfall layouts: transform, split, modal DFC
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Three-panel editor layout, search panel, and deck card tile</name>
  <files>
    src/components/deck-editor.js,
    src/components/deck-search-panel.js,
    src/components/deck-card-tile.js,
    src/screens/thousand-year.js
  </files>
  <read_first>
    src/screens/thousand-year.js,
    src/components/gallery-view.js,
    src/components/filter-bar.js,
    src/components/card-tile.js,
    src/db/search.js,
    src/db/card-accessor.js,
    src/stores/deck.js,
    src/stores/collection.js,
    .planning/phases/03-deck-builder-thousand-year-storm/03-UI-SPEC.md
  </read_first>
  <action>
    1. Create src/components/deck-editor.js exporting `renderDeckEditor(container)`:
       - Three-panel flex layout filling the content area (height: 100%, no scroll on outer container)
       - Left panel (search): fixed width 280px at >= 1280px viewport, 240px at < 1280px. Background: surface (#14161C).
       - Centre panel: flex-1, overflow-y auto. Background: background (#0B0C10).
       - Right panel (analytics): fixed width 280px at >= 1280px, 240px at < 1280px. Background: surface (#14161C).
       - Panel separation via tonal shifting (No-Line Rule), no borders between panels.
       - Back navigation: deck name as clickable breadcrumb at top of centre panel. Click dispatches `deck-back-to-landing` event.
       - Mount search panel via renderDeckSearchPanel(leftEl)
       - Mount centre panel via renderDeckCentrePanel(centreEl) (Plan 03 Task 2)
       - Right panel shows placeholder "ARCHIVE ANALYTICS" header + "Coming in Plan 04" until Plan 04

    2. Update src/screens/thousand-year.js:
       - Wire the editor mode: when `deck-open` event fires or URL has deck ID, call renderDeckEditor(container)
       - Handle `deck-back-to-landing` event: re-render landing view
       - Unlock the screen in sidebar: update the screen from locked state

    3. Create src/components/deck-search-panel.js exporting `renderDeckSearchPanel(container)`:
       - Overline: "CARD RETRIEVAL" (label 700, primary colour #0D52BD)
       - Search input: placeholder "SEARCH CARDS..." (Space Grotesk 14px). Focus ring: primary colour. 150ms debounce.
       - "IN COLLECTION ONLY" toggle button (per D-14):
         - OFF (default): filter-toggle-off class (ghost border, text-muted). Shows all cards.
         - ON: filter-toggle-on class (primary bg). Filter results to cards where scryfall_id exists in Alpine.store('collection').entries.
         - Implementation: when ON, get Set of scryfall_ids from collection store, filter search results to only those IDs.
       - Filter controls below toggle (label 700 headers):
         - TYPE: dropdown with card types (Creature, Instant, Sorcery, Enchantment, Artifact, Planeswalker, Land, All)
         - CMC: dropdown with 0, 1, 2, 3, 4, 5, 6, 7+, All
         - RARITY: dropdown with Common, Uncommon, Rare, Mythic, All
         - All filters default to "All"
       - Search execution: query searchCards() from search.js. Filter results by:
         - Commander colour identity: card.color_identity must be subset of activeDeck.color_identity
         - Type filter (if not All): card.type_line includes selected type
         - CMC filter (if not All): card.cmc matches (7+ means >= 7)
         - Rarity filter (if not All): card.rarity matches
       - Results list: scrollable container (overflow-y auto, max height fills remaining panel space)
         - Each result row: 32px card thumbnail (art_crop), card name (body 14px), mana cost via renderManaCost(), price in GBP via window.__cf_eurToGbp()
         - Hover: surface-hover background
         - Click: call Alpine.store('deck').addCard(scryfallId). Toast feedback.
         - Right-click: dispatch `deck-search-context-menu` event (Add to Deck, Add to Collection, Add to Wishlist, View Details, View on Scryfall)
       - Search results must be SortableJS-compatible for drag-to-deck (add data attributes: `data-scryfall-id`)
       - No results state: "No cards match your search within this colour identity." in text-muted

    4. Create src/components/deck-card-tile.js exporting `renderDeckCardTile(entry, options)`:
       - `entry` is a deck card object: { id, deck_id, scryfall_id, quantity, tags, card, owned }
       - Grid mode: card image (Scryfall normal or art_crop), card name below, mana cost
       - List mode: table row with name, type_line, mana cost (renderManaCost), price (eurToGbp), tags (tag-pill class)
       - Owned indicator: 6px green dot (owned-dot class) positioned absolute top-4 right-4 of tile (grid mode) or green text marker (list mode)
       - Missing indicator: 6px red dot (missing-dot class) + price displayed in label tier below card (grid mode) or red marker + price in price column (list mode)
       - Quantity badge: if quantity > 1, show "x{qty}" badge on tile
       - Tag pills: small pills below card image (grid) or in tag column (list), using tag-pill class
       - Data attributes: `data-deck-card-id="{entry.id}"`, `data-scryfall-id="{entry.scryfall_id}"`, `data-type-group="{classifyType(entry.card.type_line)}"`
       - Right-click: dispatch `deck-context-menu` event with entry data and cursor position
       - Click: dispatch `card-flyout` event to open existing card detail flyout (per D-23, reuse Phase 1/2 pattern)
  </action>
  <verify>
    <automated>cd "D:/Vibe Coding/counterflux" && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/components/deck-editor.js exports `renderDeckEditor` and contains three div containers with fixed widths (280px) for search and analytics panels
    - src/components/deck-editor.js uses surface (#14161C) for side panels and background (#0B0C10) for centre
    - src/components/deck-search-panel.js exports `renderDeckSearchPanel` and contains `CARD RETRIEVAL`, `IN COLLECTION ONLY`, `SEARCH CARDS`, filter dropdowns for TYPE/CMC/RARITY
    - src/components/deck-search-panel.js contains `color_identity` filtering logic
    - src/components/deck-search-panel.js contains `Alpine.store('collection')` reference for In Collection toggle
    - src/components/deck-card-tile.js exports `renderDeckCardTile` and contains `owned-dot`, `missing-dot`, `tag-pill`, `data-deck-card-id`
    - src/screens/thousand-year.js handles `deck-open` and `deck-back-to-landing` events
    - Build succeeds
  </acceptance_criteria>
  <done>Three-panel editor renders with search panel on left. Search filters by colour identity. In Collection toggle works. Card tiles show owned/missing dots. Clicking search result adds card to deck.</done>
</task>

<task type="auto">
  <name>Task 2: Centre panel with type groups, grid/list views, SortableJS drag-and-drop, context menu, and tag manager</name>
  <files>
    src/components/deck-centre-panel.js,
    src/components/deck-context-menu.js,
    src/components/tag-manager.js
  </files>
  <read_first>
    src/stores/deck.js,
    src/components/deck-card-tile.js,
    src/components/deck-editor.js,
    src/components/context-menu.js,
    src/components/gallery-view.js,
    src/components/table-view.js,
    src/utils/type-classifier.js,
    src/utils/tag-heuristics.js,
    .planning/phases/03-deck-builder-thousand-year-storm/03-UI-SPEC.md,
    .planning/phases/03-deck-builder-thousand-year-storm/03-CONTEXT.md
  </read_first>
  <action>
    1. Create src/components/deck-centre-panel.js exporting `renderDeckCentrePanel(container)`:

       Header section:
       - Deck name: heading tier (Syne 20px 700)
       - Card count: display tier (Syne 48px 700) showing `{cardCount}/{deck_size}` (per D-18)
       - Subtitle: "SLOTS REMAINING" count in label 400
       - Owned summary bar (per D-13): "YOU OWN {owned}/{total} -- MISSING COST: {price}" in label 400. Price via window.__cf_eurToGbp(). Compute owned count from activeCards.filter(c => c.owned).length.
       - View toggle: GRID / LIST buttons. Active uses view-toggle-active class. Inactive uses view-toggle-inactive class. Clicking updates Alpine.store('deck').viewMode.
       - Import/Export buttons: "Import Decklist" and "Export Decklist" (label 700). These dispatch events consumed by Plan 05 modals. For now, show disabled state with tooltip "Coming soon".

       Card groups (per D-04, D-17):
       - Read from Alpine.store('deck').groupedByType
       - Render in TYPE_ORDER sequence
       - Each group has a header: "CREATURES (23)" format (label 700 uppercase, card count in parentheses)
       - Headers are collapsible: click toggles expand/collapse with chevron icon (Material Symbols `expand_more` / `expand_less`). Default: expanded.
       - Skip empty groups (don't render header if 0 cards)

       Grid view (default, per D-16):
       - Cards in a responsive grid: `grid grid-cols-4 xl:grid-cols-5 2xl:grid-cols-6 gap-2` within each group
       - Each card rendered via renderDeckCardTile(entry, { mode: 'grid' })
       - SortableJS on each group container:
         ```javascript
         import Sortable from 'sortablejs';
         new Sortable(groupEl, {
           group: 'deck-cards',
           animation: 150,
           ghostClass: 'drag-ghost',
           fallbackOnBody: true,
           onEnd(evt) {
             // CRITICAL: Revert DOM change, update store, let Alpine re-render
             // Get deckCardId from evt.item.dataset.deckCardId
             // Get new group from evt.to.dataset.typeGroup
             // Call Alpine.store('deck').reorderCard(deckCardId, newSortOrder)
             // Remove the DOM element SortableJS inserted (store re-render handles it)
             if (evt.from !== evt.to) {
               evt.item.remove(); // Prevent duplicate, store re-renders
             }
           }
         });
         ```
       - Search panel drag source: use `group: { name: 'deck-cards', pull: 'clone', put: false }` on search results list. On drop into centre panel group, call addCard() and remove the cloned DOM element.
       - Drop zone highlighting: per UI-SPEC, target group header gets 2px primary left border + glow-blue box-shadow via `.drop-zone-active` class. Apply via SortableJS `onMove` or CSS `:has()`.

       List view (per D-16):
       - Compact table within each group: columns for Name, Type, Mana Cost, Price (GBP), Tags
       - Each row rendered via renderDeckCardTile(entry, { mode: 'list' })
       - No drag-and-drop in list view (per UI-SPEC)
       - Sortable columns: click header to sort within group

       SortableJS lifecycle:
       - Create Sortable instances when grid view renders
       - Destroy all instances when switching to list view or when component unmounts
       - Re-init when Alpine re-renders card groups (use MutationObserver or re-init after store updates)

    2. Create src/components/deck-context-menu.js exporting `initDeckContextMenu(container)`:
       - Listen for `deck-context-menu` custom DOM events (per D-22)
       - Follow context-menu.js pattern from Phase 2
       - Menu items (per UI-SPEC copywriting contract):
         - "Remove from Deck" (destructive, text-secondary #E23838) -- calls store.removeCard(). Toast: "{name} removed."
         - "Change Quantity" -- shows inline number input, calls store (for 60-card formats; in Commander, show warning about singleton)
         - "Add Tag" -- submenu listing deck tags from activeDeck.tags. Selecting a tag calls store.updateCardTags() to toggle.
         - "Add to Collection" -- calls Alpine.store('collection').addEntry() with card data. Toast: "Added to collection."
         - "Add to Wishlist" -- calls Alpine.store('collection').addEntry() with category 'wishlist'. Toast: "Added to wishlist."
         - "View Details" -- dispatches `card-flyout` event for existing flyout (per D-23)
         - "View on Scryfall" -- opens `https://scryfall.com/card/{set}/{collector_number}` in new tab
       - Also handle `deck-search-context-menu` events from search panel with items: Add to Deck, Add to Collection, Add to Wishlist, View Details, View on Scryfall

    3. Create src/components/tag-manager.js exporting `renderTagManager(container, deckId)`:
       - Displays current deck tags from activeDeck.tags
       - Each tag as a pill (tag-pill class) with delete "x" button
       - "+ ADD TAG" button that shows inline text input (placeholder "TAG NAME...")
       - On submit: add tag to deck's tags array in Dexie, reload
       - On delete: remove tag from deck's tags array, remove from all deck_cards that had it
       - Tag pills can be dragged to "Functional Tags" filter area (stretch - implement as clickable filter for now)
       - Per D-07: users can create custom tags, rename tags, delete tags
       - Render within the analytics panel area or as a dropdown from the centre panel header
  </action>
  <verify>
    <automated>cd "D:/Vibe Coding/counterflux" && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/components/deck-centre-panel.js exports `renderDeckCentrePanel` and contains `CREATURES (`, `SLOTS REMAINING`, `YOU OWN`, `groupedByType`, `viewMode`
    - src/components/deck-centre-panel.js contains `new Sortable(` with `group: 'deck-cards'`
    - src/components/deck-centre-panel.js contains grid view (`grid grid-cols`) and list view rendering paths
    - src/components/deck-centre-panel.js contains `view-toggle-active` and `view-toggle-inactive` classes
    - src/components/deck-context-menu.js exports `initDeckContextMenu` and contains `Remove from Deck`, `Change Quantity`, `Add Tag`, `View on Scryfall`
    - src/components/deck-context-menu.js contains `deck-context-menu` and `deck-search-context-menu` event listeners
    - src/components/tag-manager.js exports `renderTagManager` and contains `ADD TAG`, `tag-pill`
    - Build succeeds with no errors
  </acceptance_criteria>
  <done>Centre panel renders cards grouped by type with collapsible headers. Grid/list toggle works. Drag-and-drop moves cards between groups. Context menu provides all deck card actions. Tag manager allows creating, deleting custom tags. Card count and owned summary display correctly.</done>
</task>

</tasks>

<verification>
- `npm run build` -- no errors
- Three-panel layout renders with correct widths and tonal shifting
- Search finds cards filtered by colour identity
- In Collection toggle filters to owned cards only
- Adding a card from search increments count and shows in centre panel
- Grid view shows card images with type group headers
- List view shows compact table rows
- Drag-and-drop moves cards between groups in grid view
- Right-click shows context menu with correct items
- Card count tracker updates on add/remove
- Owned summary bar shows correct counts and GBP price
</verification>

<success_criteria>
- Three-panel editor is functional: search, add, organise, remove cards
- SortableJS drag-and-drop works between type groups and from search to deck
- Grid and list views toggle correctly
- Context menu provides all required actions
- Card count tracker and owned summary are live-updating
</success_criteria>

<output>
After completion, create `.planning/phases/03-deck-builder-thousand-year-storm/03-03-SUMMARY.md`
</output>
